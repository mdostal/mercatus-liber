# Build-time content sync output -- regenerated from docs/ and README.md at the repo root by
# scripts/sync-content.mjs on every predev/prebuild. Never hand-edit or commit this directory;
# docs/ at the repo root is the single source of truth.
content/

# Next.js build output
.next/
next-env.d.ts
*.tsbuildinfo

# Auto-(re)generated by `next dev`/`next build` (Next.js 16's agent-rules feature) on every run
AGENTS.md
CLAUDE.md
