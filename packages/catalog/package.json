{
  "name": "@mercatus-liber/catalog",
  "version": "0.1.0",
  "description": "Product/SKU/attribute engine (subsystem 01). Depends on @mercatus-liber/core only -- persistence is injected via CatalogPersistenceAdapter, never imported concretely, so this package works with any adapter (adapter-sqlite is a devDependency for tests only).",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mercatus-liber/core": "workspace:*"
  },
  "devDependencies": {
    "@mercatus-liber/adapter-sqlite": "workspace:*",
    "@types/node": "^22.7.5",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
