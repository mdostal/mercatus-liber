{
  "name": "@mercatus-liber/cart",
  "version": "0.1.0",
  "description": "Long-lived cart engine (subsystem 07). Depends on @mercatus-liber/core only. Reads SKU data through a narrow structural SkuLookup interface (satisfied by @mercatus-liber/catalog's CatalogService, but never imports that package) -- no dependency on checkout-orders or payments; those subsystems depend on cart, not the reverse.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mercatus-liber/core": "workspace:*"
  },
  "devDependencies": {
    "@mercatus-liber/adapter-sqlite": "workspace:*",
    "@mercatus-liber/catalog": "workspace:*",
    "@types/node": "^22.7.5",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
