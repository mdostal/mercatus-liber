{
  "name": "@mercatus-liber/pdp",
  "version": "0.1.0",
  "description": "Product Display Page data/behavior contract (subsystem 04). Depends on @mercatus-liber/core + @mercatus-liber/theming (a leaf with no adapter-swapping story, so a real import is fine); reads catalog via a narrow structural ProductLookup interface, never importing @mercatus-liber/catalog directly.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mercatus-liber/core": "workspace:*",
    "@mercatus-liber/theming": "workspace:*"
  },
  "devDependencies": {
    "@mercatus-liber/adapter-sqlite": "workspace:*",
    "@mercatus-liber/catalog": "workspace:*",
    "@types/node": "^22.7.5",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
