{
  "name": "@mercatus-liber/create-store",
  "version": "0.1.0",
  "description": "Scaffolds a new deployable Mercatus Liber store -- the deploy-tool half of the commercial thesis (epic deploy-tool-and-auto-update). Generates a package.json wired to the chosen adapter, a minimal service-composition entry point, and mercatus-liber.config.json (the manifest @mercatus-liber/auto-update reads).",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "bin": {
    "create-mercatus-liber-store": "./dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mercatus-liber/theming": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
