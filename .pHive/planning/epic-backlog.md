# Epic Backlog — the full vision, broken into drainable chunks

_Written 2026-09-07. This is the thing that "drains" — status updates here as each epic is
planned/executed. Sequenced so each epic leaves the repo in a genuinely working state (Hive's
vertical-slice invariant), not just "some files exist."_

| # | Epic ID | Covers | Status | Depends on |
|---|---|---|---|---|
| 1 | `core-foundation` | 00 core schema, `adapter-sqlite` (reference DB adapter), 01 catalog, 07 cart, 08 payments (Stripe), 09 checkout-orders, reference-storefront integration. The smallest real vertical slice: browse a seeded catalog → add to cart → checkout via Stripe → order recorded. | **done** (all 7 stories complete) | — |
| 2 | `marketing-catalog-search` | 02 marketing catalog, 03 search (default in-repo index adapter) | **done** (all 3 stories complete) | 1 |
| 3 | `pdp-theming` | 04 PDP (2+ layout configs), 06 theming/layout system | **done** (all 3 stories complete) | 1, 2 |
| 4 | `cms-pages` | 05 CMS — all 5 page types (home, category, marketing/campaign, search, PDP wiring), component registry | **done** (2/2 stories complete) | 2, 3 |
| 5 | `customer-account` | 10 account — profile, dashboard, order tracking | **done** (2/2 stories complete) | 1 |
| 6 | `inventory` | 11 inventory — in-house adapter + external-IMS adapter contract | **done** (2/2 stories complete) | 1 |
| 7 | `plugins-extensibility` | 12 plugins — plugin lifecycle, event-bus extension points, one reference plugin | **done** (2/2 stories complete) | 1 |
| 8 | `adapter-postgres` | Second reference DB adapter (Postgres) — proves the persistence interface is truly adapter-agnostic | **done** (1/1 story complete) | 1 |
| 9 | `six-themes` | 6 optional drop-in themes built on the theming system (06) — the "roll it with 6 optional themes" deliverable | **done** (2/2 stories complete) | 3, 4 |
| 10 | `admin-janus-dogfood` | Admin view for catalog/CMS/orders management — attempt to build it via Janus's composer (dogfood), fall back to hand-built admin UI documented as a subsystem-12-style plugin if Janus integration isn't viable | **done** (2/2 stories complete — Janus dogfood documented non-viable, hand-built /admin shipped) | 1, 4, 6 |
| 11 | `shop-migration` | Migrate `shop.mdostal.com` off its own custom cart onto these packages — the "proof by use" success criterion from north_star | **done** (2/2 stories complete, in the `shop` repo -- see `shop/.pHive/epics/mercatus-liber-storefront/`) | 1 (minimum), ideally 2-4 |
| 12 | `analytics-tracking` | 13 analytics — event-bus subscriber, PostHog adapter enabled by default, client-side `trackEvent()` helper | **done** (2/2 stories complete) | 1 |
| 13 | `ai-mcp-interface` | 14 AI/MCP interface — MCP server + skills/tool catalog wrapping catalog/cart/checkout (shopper side) and catalog/CMS/inventory (admin side) | **done** (2/2 stories complete) | 1 (shopper side), 4 + 6 (admin side) |
| 14 | `adapter-shopify` | A **Shopify commerce-backend adapter** — proves the persistence/commerce-backend interface can wrap an entire third-party platform, not just a raw DB. Lets a client already on Shopify adopt Mercatus Liber's admin/AI-agent/plugin layer *without* migrating off Shopify. | **done** (1/1 story complete) | 1, 8 (pattern proven by adapter-postgres first) |
| 15 | `att-recreation-acceptance-test` | **Redefined 2026-09-08 (Mathew's explicit correction: "we are NOT updating their site, we are making a clone of it as an EXAMPLE/DEMO").** Two variants, both built on epics 17-19: **(a)** an internal-only, non-public exact clone using All That Technology's real identity/content — for presenting directly to that client, never deployed to a public URL; **(b)** a public, obfuscated demo theme (fictional business name/logo, a similar-but-not-identical look) proving the same service-area/home-automation feature set as a giveaway example. **Both variants done** — see 15a/15b below. This is the actual finish line for the vision (see "Definition of vision fully done" below). | **done** (both variants complete) | 2, 4, 6, 14, 17, 18, 19 |
| 15a | `att-private-clone-internal` | The internal-only exact clone (variant a of epic 15). Real ATT branding/content sourced from their own real site-planning doc (`att-site/docs/internal/site-context.md`) — business name, real confirmed service list, real confirmed 8 service-area cities, real confirmed "Trust-First" homepage direction and "Clean Slate" color scheme — deliberately excluding all revenue/financial data, ad account IDs, analytics IDs, and credentials (out of scope for a site recreation). | **done** — built in a brand-new, separate, non-public repo (`~/Documents/work/clients/att-recreation-internal`, own local git history, **no remote configured**), never mercatus-liber's own history (which is destined for eventual OSS/public release — keeping real client content out of it entirely is a stronger safety boundary than an in-repo "internal" folder). See that repo's own README.md (loud do-not-deploy banner) and `docs/what-this-uses-and-doesnt.md` (exact content boundary). Verified live end-to-end (browsing/cart/PDP/locations render real data; checkout fails loudly without a Stripe key), same rigor as epic 11. | 15, 17, 18, 19 |
| 15b | `service-demo-theme-public` | The public demo (variant b of epic 15). Fictional business identity (name + logo + a theme visually similar to, but distinct from, ATT's real look), same home-automation/service-area feature set, plugins/analytics fully wired, deployable as the framework's flagship give-away example. | **done** (2/2 stories complete — "Northline Home Tech", see `.pHive/epics/service-demo-theme-public/`) | 15, 17, 18, 19 |
| 16 | `deploy-tool-and-auto-update` | The **commercial thesis**: a one-command installer to stand up a store on a client's own infrastructure (not just Mathew-hosted), plus an auto-update mechanism for security/maintenance patches — what makes "$1,000 one-time setup, remove folks from Shopify" (e.g. client Cadex) actually operable at more than one client without ongoing hand-holding. | **done** (2/2 stories complete) | 1, 9 |
| 17 | `commerce-gap-audit` | **Added 2026-09-08.** Audit every plugin/analytics/CMS touchpoint across the repo, close small real gaps found (e.g. missing client-side analytics events), and write up what's genuinely missing as new backlog epics (20-23 below) rather than bolting them on ad hoc. Prerequisite for 15a/15b so the demo actually proves real coverage, not just what happened to get built first. | **done** (1/1 story complete) | 12 |
| 18 | `cms-content-adapters` | **Added 2026-09-08 (Mathew's explicit ask: "sanity CMS and other parts need to be adapters as well and wrapped").** Formalize CMS's `PageRepository`/`MarketingPageMetaRepository` as an explicit adapter contract (same shape as `CatalogPersistenceAdapter`), and build a reference third-party CMS adapter (`adapter-sanity`, wrapping Sanity's Content API) proving a client's existing CMS/design-system tooling (including Next.js's own Vercel-toolbar-integrated visual-editing tools) can sit alongside or replace the built-in CMS without touching any subsystem that depends on it. | **done** (2/2 stories complete) | 4 |
| 19 | `service-areas-location-pages` | **Added 2026-09-08 (Mathew's explicit ask: "ensure we can make the location based pages for service area").** New subsystem (service-areas: city/region entities distinct from marketing-catalog's product categories, same "data vs. page layout" split as 02/05) + a new CMS `location` page type + reference-storefront `/locations/[slug]` route -- the multi-location marketing pattern ATT's real business needs (8 core cities). | **done** (2/2 stories complete) | 2, 4 |
| 20 | `promotions-discounts` | Coupon codes, percentage/fixed cart- and product-level discounts. New subsystem 16 (`@mercatus-liber/promotions`, core-only dependency) resolved design question: own subsystem, not a checkout-orders extension or a cart addition -- satisfies an optional `PricingAdjuster` structural interface checkout-orders declares in its own `types.ts`. Admin CRUD at `/admin/promotions` (first admin-side mutation UI). See `docs/subsystems/16-promotions.md` and `.pHive/epics/promotions-discounts/docs/design-discussion.md`. | **done** (4/4 stories complete) | 7, 9 |
| 21 | `bundles` | Multi-SKU tiered package selector (e.g. "Product Only" / "+ Pro Setup" / "Complete Overhaul") sold from a single PDP. New subsystem 17 (`@mercatus-liber/bundles`, references SKUs by id only, never forks/extends `Product`/`Sku`) resolved design question: a tier selection becomes N ordinary per-SKU cart lines orchestrated at the app layer, not a new line-item shape cart/checkout-orders/promotions/inventory have to learn. Admin CRUD at `/admin/bundles`. See `docs/subsystems/17-bundles.md` and `.pHive/epics/bundles/docs/design-discussion.md`. | **done** (4/4 stories complete) | 1 |
| 22 | `upsell-cross-sell` | "Customers also bought" / PDP and cart recommendations. New subsystem 18 (`@mercatus-liber/recommendations`, core-only dependency) resolved design question: an explicit, admin-authored source-product -> target-products mapping, never inferred/computed, distinct from marketing-catalog's `SuggestionRule` (which suggests *categories* for a product, not products for a shopper) -- a genuinely new recommendation surface. PDP/cart resolution and the same-category fallback are app-composed orchestration, not part of the package itself. Admin CRUD at `/admin/recommendations`. See `docs/subsystems/18-recommendations.md` and `.pHive/epics/upsell-cross-sell/docs/design-discussion.md`. | **done** (4/4 stories complete) | 1, 4, 9 |
| 23 | `advertising` | CMS already ships a static `ad-slot` component (subsystem 05); this epic added the management layer that fills it -- new subsystem 19 (`@mercatus-liber/advertising`, core-only dependency) resolved design question: an admin-curated Campaign/Creative model with optional service-area/page-slug targeting and stateless weighted-random rotation, resolved at the app-composition layer (`cms-sections.tsx`'s AdSlot), never inside `packages/cms` itself. Admin CRUD at `/admin/advertising`. See `docs/subsystems/19-advertising.md` and `.pHive/epics/advertising/docs/design-discussion.md`. | **done** (4/4 stories complete) | 4 |
| 24 | `internal-bi-metrics` | **Backlogged 2026-09-08 (from the 20260908 hand-off brief).** Subsystem 13 (`@mercatus-liber/analytics`) forwards events to external tools (PostHog) -- that's marketing/tracking analytics, write-only, no read side. There is no owned, internal business-intelligence layer: revenue over time, order volume, top products/services, conversion funnel, inventory turns, promotion redemption rates. This epic builds that as its own subsystem -- new subsystem 20 (`@mercatus-liber/internal-bi`) resolved design question: an explicit, contract-shaped `BiMetricsAdapter` (mirroring `CatalogPersistenceAdapter`/`CmsPersistenceAdapter`) so a deployment can plug in a real BI tool later, with `createDefaultBiAdapter` as the zero-infra reference implementation reading straight from the existing persistence adapters (catalog/checkout-orders/inventory/promotions) plus a new event-log-backed conversion funnel. Required one additive, backward-compatible field, `Order.createdAt` (bi-01), the same low-risk pattern as prior `Order` extensions. Surfaced as a real dashboard at `/admin/metrics`, live-verified against genuine cart/checkout activity on a running dev server. See `docs/subsystems/20-internal-bi.md` and `.pHive/epics/internal-bi-metrics/docs/design-discussion.md`. | **done** (4/4 stories complete) | 1, 6, 9, 20 |
| 25 | `admin-adapter-visibility-settings` | **Backlogged 2026-09-08 (from the 20260908 hand-off brief).** A settings page in `/admin` showing which concrete adapter is active for each swappable subsystem (persistence, CMS, payments) -- visibility into "how this instance is actually running," making the "adapters and options for how it runs" pitch concrete in the UI itself. Resolved design question: no new `packages/*` subsystem -- this is metadata about this one app's own composition choices, not a reusable cross-deployment domain capability, so it lives as a small hand-maintained `apps/reference-storefront/lib/adapter-info.ts` descriptor (`getAdapterInfo()`) next to `services.ts`'s own `create*Adapter(...)` calls instead. Surfaced at `/admin/settings` (plus a bonus analytics row), live-verified on a running dev server: payments genuinely shows "unconfigured" in this environment (no `STRIPE_SECRET_KEY` set), analytics never does. See `.pHive/epics/admin-adapter-visibility-settings/docs/design-discussion.md`. | **done** (2/2 stories complete) | 1, 18 |
| 26 | `commerce-gap-audit-2` | **Added 2026-09-08.** A second audit, mirroring epic 17's own methodology, run after epics 20-25 landed -- checked payments' adapter swappability, every open question across the six new subsystem docs (16-20), cross-subsystem composability, plugin/analytics event coverage, version/changelog state, and admin nav growth. **Conclusion: no new backlog epic was warranted** -- every real finding was either already correctly disclosed as deferred/blocked, or small enough to fix directly (page-view/impression tracking on the surfaces that didn't exist when epic 17 ran, a real integration test proving bundles+promotions compose, one missing analytics event mapping, and this repo's first `CHANGELOG.md` + version bump, scoped to epics 20-25 only). See `.pHive/epics/commerce-gap-audit-2/docs/audit-findings.md`. | **done** (1/1 story complete) | 20, 21, 22, 23, 24, 25 |
| 27 | `admin-auth-clerk` | **Added 2026-09-08, from a fresh gap audit that found `/admin` had zero authentication anywhere in the repo** -- no `middleware.ts`, no layout guard, no role concept at all, a genuine, live-exploitable gap on a real public deployment (`demo-shop.mdostal.com`'s `/admin` was fully open, full CRUD, no gate), never previously scoped in or out by any prior epic. This epic builds the real fix, not a stopgap: new subsystem 21 (`@mercatus-liber/admin-auth`, core-only dependency) declares the swappable `AdminAuthAdapter` contract plus a zero-infra local dev default, mirroring `CatalogPersistenceAdapter`/`CmsPersistenceAdapter`/`BiMetricsAdapter`'s exact adapter shape, with the real, deployed, production-grade implementation living in a new sibling package, `@mercatus-liber/adapter-clerk`, wrapping Clerk's Next.js and Backend SDKs -- the user's explicit, deliberate choice ("we will be building on clerk 100% i love clerk"), not a hypothetical generic-provider abstraction. A three-role model (owner/admin/viewer) resolves permissions via a small pure `hasPermission` check, with role data living in Clerk's own `publicMetadata` rather than a second local database. Enforced two-layered -- root `middleware.ts` for authentication, `app/admin/layout.tsx` plus a `requireAdminPermission` guard on every existing admin mutation action for authorization -- and surfaced with a new owner-only `/admin/settings/users` management page. See `docs/subsystems/21-admin-auth.md` and `.pHive/epics/admin-auth-clerk/docs/design-discussion.md`. | **done** (5/5 stories complete, live verification against a real identity-provider account pending real credentials) | 1, 10 |
| 28 | `cms-admin-crud` | **Added 2026-09-08, from a fresh gap audit finding: `packages/cms`'s `CmsService.createPage`/`updatePage`/`publishPage`/`createMarketingPage` all already worked, but nobody had ever built an admin UI to call them (`/admin/cms` was a read-only table) and the `manage_cms_page` MCP tool only supported `"update"`/`"publish"`, never `"create"`** -- neither a human admin nor an agent could author a new CMS page (including a marketing/campaign page) without hand-editing seed code. No new package or subsystem was needed -- `CmsService`'s existing public surface already had everything this epic required. Closed both gaps: `/admin/cms/new` and `/admin/cms/marketing/new` (a fixed-slot, raw-JSON-config section editor, mirroring bundles'/advertising's own plain-HTML convention) plus an edit/publish flow at `/admin/cms/[id]`, and a new `"create"` action on the existing `manage_cms_page` MCP tool, gated by the same `requireAdminPermission("mutate")` guard every other admin mutation carries. Live-verified end-to-end on a real dev server: a real category page (`category-spot` section referencing the seeded `desk-accessories` category) created as draft, edited, and published; a real marketing/campaign page (`fall-desk-refresh`, curated with the two real seeded Dragon Cable Organizer/Dragon Desk Mat products) created, published, and rendering correctly at its own `/campaign/fall-desk-refresh` route; the MCP tool's create action confirmed via its existing test suite (preview-only without `confirm:true`, real creation with it); and a viewer-role session confirmed rejected on all 4 new mutation actions. See `.pHive/epics/cms-admin-crud/docs/design-discussion.md`. | **done** (3/3 stories complete) | 4, 13, 27 |
| 29 | `cms-adapter-discoverability` | **Added 2026-09-08, from a fresh gap audit finding: `@mercatus-liber/adapter-sanity` (epic 18) was already real, tested code, but `apps/reference-storefront/lib/services.ts` never actually constructed it** -- CMS persistence was hardcoded to the in-memory adapter with zero env-var branch, and README documented nothing about it, so Sanity was functionally present but practically invisible. This was a discoverability gap, not a capability gap -- adapter-sanity itself needed no changes. Wired `SANITY_PROJECT_ID` (with optional `SANITY_DATASET`/`SANITY_TOKEN`) into `lib/services.ts` with the same env-var-truthy-picks-the-real-adapter-else-a-harmless-fallback shape already used for analytics/admin-auth, added `@mercatus-liber/adapter-sanity` as a real dependency of `apps/reference-storefront/package.json`, documented the three env vars in a new "Content (CMS)" README Configuration subsection, and fixed `lib/adapter-info.ts`'s `/admin/settings` CMS row, which still carried a stale "no env branch exists for this today" detail string -- it now correctly reports Sanity/active when `SANITY_PROJECT_ID` is set and the in-memory default/active otherwise, mirroring the payments/analytics rows' own two-state pattern. Sanity is now a real, documented, env-var-selectable option. `create-store`'s scaffolder was explicitly left unchanged -- it doesn't generate CMS wiring for any adapter today, disclosed as separate future scaffolder work, not part of this discoverability fix. Live-verified on a real dev server with no Sanity env vars set: `/admin/cms`, home, and `/campaign/fall-sale` all rendered identically to before, and `/admin/settings`'s CMS row correctly showed the in-memory default. See `.pHive/epics/cms-adapter-discoverability/docs/design-discussion.md`. | **done** (2/2 stories complete) | 18, 25 |
| 30 | `create-store-skill` | **Added 2026-09-08.** `@mercatus-liber/create-store` (the scaffolder underpinning epic 16's commercial thesis) had real code but no real README and no agent-facing procedure for using it. This epic gave it both: a corrected, code-verified `packages/create-store/README.md` (CLI flags, the three adapter env vars, the full current theme-key table, exactly what a scaffold does and doesn't produce) and `.claude/skills/create-store/SKILL.md`, an agent-facing skill for provisioning a new store. Resolved design question: a Claude Code skill, not a new MCP tool -- store provisioning is a build-time action against this monorepo checkout (run the CLI, edit the generated `package.json`, install, typecheck), not a runtime store-operation an already-running store's MCP server would expose. Closeout story literally followed the written SKILL.md procedure end to end -- scaffolded a real throwaway store outside this repo (sqlite adapter, `northline` theme), including the real, verified workaround these docs disclose because `@mercatus-liber/*` packages aren't published to npm yet (rewriting the generated `package.json`'s `@mercatus-liber/*` ranges to local `link:` paths into this checkout's `packages/*`, plus adding `devDependencies`/`tsconfig.json` the scaffold doesn't generate) -- and confirmed a clean `pnpm install` and `npx tsc` (exit 0, no output) with zero corrections needed to the written docs. The throwaway store was deleted afterward and never appeared in this repo's `git status`. | **done** (2/2 stories complete) | 16 |
| 31 | `commerce-landing-and-demo-routing` | **Added 2026-09-09.** The largest single body of work this session (~150 files touched across its 6 stories) -- gave `commerce.mdostal.com`'s root a real, demo-agnostic framework landing page instead of defaulting straight into a single hardcoded demo's shop, and made both reference demos (`dragon-merch` and Northline/`att-recreation-acceptance-test`'s public variant) genuinely, simultaneously live under one deployment instead of one-brand-at-a-time via a build-time env var. Resolved multi-tenancy architecture: every shopper- and admin-facing route physically moved under `app/demo/[demoSlug]/...` (route param, not env var, picks the demo); `lib/services.ts`'s old single-value services singleton became a `Map<DemoSlug, Promise<Services>>`-keyed `getServicesForDemo()` registry so each demo gets its own fully independent, lazily-built service graph; and every cart/session-bearing cookie (cart id, admin dev session usage) is now demo-namespaced (e.g. `ml_cart_id__dragon-merch` vs. `ml_cart_id__northline`) so one browser session can hold independent state per demo at once. Closeout story live-verified all of it against a real dev server: `/` renders the demo-agnostic landing page with correct links to both demos and no shop content; `/demo/dragon-merch` and `/demo/northline` render genuinely distinct shops (different products/services, different nav, different admin catalog data); the multi-tab curl/cookie cart-isolation test added a real, different item to each demo's cart in the *same* browser session and confirmed each `/cart` page shows only its own item; `/demo/dragon-merch/admin` redirects unauthenticated and both demos' admin render correctly (with distinct data) once authenticated via the dev-default admin session; a small pre-existing bug unrelated to the route move -- three hardcoded, un-prefixed ad-campaign `linkHref` values left over from the `advertising` epic (18) in `lib/seed.ts`'s dragon-merch seed data -- was found and fixed to `/demo/dragon-merch/...`-prefixed paths, confirmed rendering correctly live; and the full monorepo test suite (56/56 tasks) and typecheck are green. A final grep for the temporary "TEMPORARY: hardcoded until routes move" shim comment introduced early in this epic found zero live occurrences -- the only surviving match is a deliberate historical comment in `mcp-server.ts` that quotes the old text *while explaining its own removal*, not a leftover shim. | **done** (6/6 stories complete) | 4, 15, 20, 21, 22, 23, 25, 27, 28 |
| 32 | `storefront-visual-redesign` | **Added 2026-09-09, built directly on top of epic 31's routing/structure work.** Epic 31 gave the demo storefronts real, isolated routing and admin data but explicitly left "actually look polished/branded" as pending -- this epic did that follow-up. Four stories: (redesign-01) enriched `packages/theming/src/theme-bundles.ts`'s token vocabulary well past the original six tokens (`--color-background/text/primary/accent/font-family/radius`) with `--color-muted`, `--color-border`, a 4-step `--space-xs/sm/md/lg` spacing scale, a `--font-size-heading-lg/md/body` type scale, and `--shadow-card`, and refined the `classic` bundle (the actual default every demo loads) into a genuinely different palette/type/spacing system -- a warm ivory/near-black print-catalog palette on a serif font stack, not the prior generic app-style flat white/system-ui/blue-600 combination -- while leaving the other six bundles (including `northline`, deliberately) untouched; (redesign-02) a copy pass across `apps/reference-storefront/lib/seed.ts` and several page/component files that found and fixed the framework's own developer pitch ("Mercatus Liber -- The 100% free, open-source, headless, AI-agent-accessible e-commerce kit") leaking into the dragon-merch shop's own hero banner -- copy that belongs on the separate framework landing page (epic 31), not inside a demo shop -- plus several other developer-facing text leaks (literal template-key debug text on the PDP, a doc-file citation on the account page, a source-path citation on order-confirmation, and a framework-pitch page `<title>`/description), and rewrote thin/generic product and category descriptions to read like real shop copy; (redesign-03) applied the enriched token vocabulary across every shared render component (`cms-sections.tsx`, PDP templates, `bundle-tier-selector.tsx`, `recommendation-shelf.tsx`, cart/category/search pages), with every new-token `var()` reference carrying a literal CSS fallback value since only `classic` defines these tokens; (redesign-04, this row) closeout live-verification. Live-verified against a real dev server (after discovering and rebuilding a stale `packages/theming/dist` build that was still serving pre-refined token values): dragon-merch's home page, a PDP (`dragon-cable-organizer`), the cart page, a category page, and a search-results page all render real shop copy (zero "headless"/"AI-agent-accessible"/"open-source" leakage) with the new CSS custom-property names (`--color-border`, `--space-xs/sm/md`, `--font-size-heading-lg/md/body`, `--shadow-card`, etc.) genuinely present as `var(--token, fallback)` references inside fetched inline `style` HTML, not just defined and unused (cart's border/shadow-card block is conditional on non-empty cart contents -- confirmed wired by reading `cart/page.tsx`'s source directly, since curl couldn't easily drive React Server Action form submissions to populate a cart). Switching to the `dark` bundle via its cookie (the same mechanism the theme-switcher UI writes) rendered all five page types with no error markers and the documented fallback values printed literally (e.g. `var(--color-border, #ddd)`, `var(--shadow-card, none)`), proving the fallback pattern genuinely works, not just in theory. `/demo/northline` was confirmed unaffected: its own theme bundle and seed data are untouched by any commit in this epic, and fetching it live with its own theme cookie set reproduces its exact pre-epic token values and copy. Full monorepo test suite (56/56 turbo tasks, 62 reference-storefront tests) and typecheck are green. **Honest disclosure: this verification was done via fetched rendered-HTML and inline-style/grep inspection against a real running dev server, not a screenshot or browser visual-review tool -- none is available in this environment -- so this is not a claim that a pixel-perfect design review took place.** | **done** (4/4 stories complete) | 31 |
| 34 | `storefront-design-system-v2` | **Added 2026-09-09, from explicit user rejection of epic 32's redesign ("is this a joke?") -- epic 32 was a token/copy pass, not real design work.** Ran the user's own stated repeatable methodology end to end: 3 genuinely independent (non-fork) design agents, same real structural brief + real seeded data (no lorem ipsum), one distinct creative direction each, each loaded `artifact-design` and shipped one self-contained HTML file with zero cross-visibility; orchestrator personally verified all 3 (balanced markup, no duplicate IDs, correct cart math, no lorem ipsum, no disallowed external resources) before showing the user anything; all 3 published as Artifacts unfiltered, no pre-selected favorite; the user's own element-level synthesis became the literal acceptance-criteria spec for implementation. Three stories: (v2-01) registered real competing `LayoutTemplate`s in `packages/theming` for 4 page types that previously had exactly one hardcoded layout each (`nav`, `home`, `category`, `cart` -- `pdp` already had 2 from an earlier epic), each new page type's *first-registered* template extracted verbatim as today's current layout so all 7 pre-existing bundles keep rendering byte-for-byte unchanged, plus 3 new `ThemeBundle`s (`editorial`/`maximalist`/`datasheet`) with full token sets pulled directly from the 3 published designs' own CSS custom properties, including one new additive token (`--font-family-display`, `editorial`-only, always referenced with a literal fallback); (v2-02) wired the actual shared render components (nav, home, category/PLP, cart) to branch on `resolveTemplate(pageType)` via a `Record<templateKey, Component>` lookup, mirroring PDP's own pre-existing pattern exactly, and added real, structurally-distinct markup per new template key (`nav.rail`'s fixed left-rail chrome, `home`/`category.magazine-grid`'s asymmetric feature-card layout, `home`/`category.spec-grid`'s hairline-border grid, `cart.receipt-style`'s monospace itemized receipt, `cart.spec-table`'s literal data table) -- also added the previously-missing `updateCartItemQuantityAction`/`removeCartItemAction` app-layer server actions (the underlying `CartService` methods already existed and were tested, just never wired to a form) and real per-line quantity-update/remove-line forms across all 3 cart templates; (v2-03, this row) extended the theme-switcher UI with the 3 new bundles under their real design names ("The Slow Catalog"/"Blaze Theme"/"Datasheet Storefront" alongside the bundle key, same cookie mechanism, no new persistence path), then closed out with a full live-verification pass against a real dev server. **5 page types now carry real, component-level template variation** (`pdp` plus the 4 newly-covered `nav`/`home`/`category`/`cart`), not just token values. Live-verified: each of the 3 new bundles across all 5 surfaces (nav via the layout, home, a category page, a PDP, and the cart with real items) rendered its bundle's distinct registered template with no error markers and no unresolved `var(--x)` fallbacks (`maximalist`'s `home`/`category`/`cart` intentionally resolve to the *standard* templates by design -- no competing structural template was proposed for that direction beyond its rail nav, so its distinctiveness there is token-only, same as before this epic for those 3 surfaces); `classic` and `northline` re-verified across the same 5 surfaces and confirmed byte-for-byte-equivalent to before this epic, proving `resolveTemplate()`'s fallback path; a real cart round-trip (add, quantity-update, remove-line, re-add, checkout) was driven via real progressively-enhanced React Server Action form submissions (not just visual inspection) against all 3 new cart templates, with correct math at every step (e.g. $29.99 x 5 = $149.95, x 7 = $209.93, x 6 = $179.94) and identical checkout behavior across every bundle including `classic` (fails the same pre-existing way in this dev environment with no `STRIPE_SECRET_KEY` configured -- an environment/config condition orthogonal to this epic, not a regression). Full monorepo test/typecheck/build via the root turbo commands are all green (56/56 typecheck, 31/31 build, 56/56 test tasks / 62 reference-storefront tests), `apps/docs` and `apps/reference-storefront` included. **This closes the "is this a joke?" gap epic 32 explicitly left open: epic 32 enriched tokens and refined the `classic` palette but never touched layout -- every bundle rendered through identical component markup. This epic makes 3 of the 10 bundles genuinely, structurally different storefronts, switchable live, with the other 7 provably unchanged.** | **done (3/3 stories complete)** | 32 |
| 35 | `demo-store-northline-depth` | **Added 2026-09-09.** User's explicit critique: "these aren't themed, they aren't integrated, we hardly have actions, it should have a true menu, a true set of products" -- applies to Northline Home Tech first. Three stories: (depth-01) rewrote `lib/seed-northline.ts` to replace the single catch-all "installation-services" category with 4 real ones (`tv-home-theater`, `security-cameras`, `networking-fiber`, `smart-home-automation`), each with >=1 real service, adding 2 new services (Smart Thermostat Installation, Smart Lock Installation) so the 4th category isn't empty; gave `security-camera-install` real tiered SKUs (1-camera $299 / 4-camera $899 / 8-camera $1,599, escalating sub-linearly) and `home-theater-setup` a second tiered example (essentials $349 / premium $549); and published a real, distinct CMS location page for all 8 `DEMO_SERVICE_AREAS` entries (previously only 1 of 8, Cedarbrook), each with real varied hours, a hand-written blurb, and a `servicesOffered` list computed directly from real `areaIndices` membership so no page can ever claim a service it doesn't actually offer; (depth-02) gave `NavTopBar`/`NavRail` a real `navLinks` prop built server-side per demo from live data (top-level categories, a Service Areas link when the demo has any, and real published CMS marketing/campaign pages) -- in the process found and fixed a real bug: both nav components carried a hardcoded `campaign/fall-sale` link that was dragon-merch-only content leaking into every demo including Northline, which has no Fall Sale campaign; (depth-03, this row) closeout live-verification. Live-verified against a real dev server: all 4 Northline categories and all 8 location pages rendered real, distinct content browsed via the actual nav links (not direct URL access), including confirming Harborview, ME's page correctly omits fiber/home-theater/thermostat/lock, services it was never assigned; a real cart round-trip added both a single-tier service (TV Wall Mounting, $149) and a specific tiered-variant SKU (Security Camera Installation's 4-camera tier, $899, out of its 3 real tiers) with correct cart math ($1,048.00 total) via real progressively-enhanced React Server Action form submissions, then a real checkout attempt failed loudly at the same expected point as every other demo in this environment (`Error: Neither apiKey nor config.authenticator provided`, Stripe key absent -- success, not a bug); the "camera" and "fiber" search queries returned exactly Security Camera Installation and Fiber Internet Installation respectively; and dragon-merch was re-confirmed completely unaffected -- its nav (Merch/3D Printed/Service Areas/its own real Fall Sale campaign link) and both its categories all still return 200 with unchanged content. Full monorepo test/typecheck/build via the root turbo commands are all green (56/56 test tasks / 65/65 reference-storefront tests, 56/56 typecheck tasks, 31/31 build tasks). `docs/subsystems/15-service-areas.md` updated to cite this epic's real seeded numbers (5 of 8 areas for fiber, all 8 for TV mounting) in place of its prior hypothetical example. | **done (3/3 stories complete)** | 34 |
| 36 | `demo-store-print-shop-rebrand` | **Added 2026-09-09.** Reconceives `dragon-merch` into "The Print Shop" -- not a rename, a genuinely different business: embroidery, custom prints/designs, coasters, and related made-to-order goods. Three stories: (print-shop-01) a genuine, mechanical slug rename `dragon-merch` -> `print-shop` across `DEMO_SLUGS`/`DEMO_REGISTRY`, every live code/test/README/VISION.md reference (confirmed by a hard zero-match repo-wide grep), and demo cookie namespacing (no migration needed -- namespaced by slug at read/write time), leaving old dragon-themed product content in place under the new slug for that story only; (print-shop-02) replaced the entire seed catalog with a real embroidery/custom-print business across 4 real categories -- Embroidery (embroidered canvas tote, embroidered dad cap), Custom Coasters (monogram stoneware set, cork-backed print set), Apparel (embroidered fleece hoodie, embroidered cotton tee), Drinkware (custom-printed ceramic mug, custom-printed travel tumbler) -- 8 real products, zero lorem ipsum, 6 of them flagged customizable; added an additive, optional `customizationNote?: string` field threaded end-to-end (PDP personalization text input on customizable products -> `addToCartAction` -> `CartItem` -> cart page display -> `OrderLineItem` -> order record), scoped deliberately to one free-text field per design-discussion.md §1b rather than a full design-asset/upload subsystem (that stays a VISION.md community-contribution item); and added `DemoDefinition.defaultThemeKey` (print-shop -> `editorial`, northline -> `northline`) with `readActiveThemeBundle()`'s cookie -> demo-default -> `THEME_BUNDLES[0]` fallback chain, retroactively giving Northline a real default theme too; confirmed epic 35's demo-aware `navLinks` mechanism auto-surfaced the new categories with zero new nav code; (print-shop-03, this row) closeout live-verification. Live-verified against a real dev server: `/demo/print-shop` renders the new catalog/nav (Embroidery/Custom Coasters/Apparel/Drinkware, plus its own Fall Sale campaign link) while `/demo/dragon-merch` is a genuine 404; a real add-to-cart submission (a real, no-JS Next.js Server Action form POST via curl, not a mocked flow) for the Embroidered Canvas Tote Bag with personalization text "Text: Sarah -- thread color: navy" produced a cart line showing that exact text on the real cart page, and a real checkout attempt reached order creation (which happens before the Stripe payment-session call) and failed at the same expected point as every other demo in this sandboxed environment (`Error: Neither apiKey nor config.authenticator provided`, Stripe key absent -- success, not a bug); fresh no-cookie visits confirmed print-shop opens on the exact editorial token set (`--color-primary: #B14B2A`) and northline on the exact northline token set (`--color-primary: #1e40af`), while an existing theme cookie (tested with `vibrant`) still overrode both, confirming the fallback chain's precedence is correct in both directions; Northline was re-confirmed completely unaffected -- all 4 categories, all 8 location pages, cart, search, and account routes still return 200 with unchanged content. This live-verification pass caught and fixed a real bug during print-shop-02: `cart/page.tsx`'s final line-mapping destructure silently dropped `customizationNote` even though it was threaded everywhere else -- TypeScript didn't catch it because the field is optional, which is exactly why this epic required live verification and not just typecheck/test. Full monorepo test/typecheck/build via the root turbo commands are all green (56/56 test tasks / 65/65 reference-storefront tests, 56/56 typecheck tasks, 31/31 build tasks). A fresh repo-wide grep confirmed zero remaining `dragon-merch` references in any live code, test, README.md, or VISION.md file (historical `.pHive/epics/<already-done-epic>/` planning docs correctly still mention it, as project history). | **done (3/3 stories complete)** | 34 |
| 37 | `demo-store-plant-shop` | **Added 2026-09-09.** A brand-new third demo store -- "somewhat eclectic but a classic commerce store," user's own example "an etsy neat plant store or something" (illustrative, not literal -- an artisan/handmade-goods-marketplace-style shop). Two stories: (plant-shop-01) added `lib/seed-broadleaf.ts` and registered a new `broadleaf` slug in `lib/demos.ts`'s `DEMO_REGISTRY`/`DEMO_SLUGS` ("Broadleaf & Co."), 9 real products across 4 real categories -- Plants, Ceramics & Planters, Textiles & Fiber Arts, and Paper & Ephemera -- zero lorem ipsum, plus a real "home" CMS page with hero-banner and product-grid sections; the Trailing Pothos ships real tiered pot-size variants (small $12 / medium $18 / large $32), one `catalog.generateSkus` call per tier so each tier keeps its own real price, mirroring `lib/seed-northline.ts`'s `ServiceTier` convention; `defaultThemeKey` set to `vibrant` (epic 34's warm orange/pink bundle, not yet any other demo's default); (plant-shop-02, this row) closeout live-verification. Live-verified against a real dev server: all 4 broadleaf categories and 3 real PDPs -- including the tiered Trailing Pothos, whose 3 distinct SKU prices ($12.00/$18.00/$32.00) all rendered and were selectable -- browsed via the real nav (not direct URL access); a real add-to-cart of the large-10in tiered variant plus a real checkout attempt reached the same expected failure point as every other demo in this sandboxed environment (`Error: Neither apiKey nor config.authenticator provided`, Stripe key absent -- success, not a bug); a fresh no-cookie visit confirmed the `vibrant` theme's exact token set (`--color-primary: #f97316`) is active by default; print-shop and northline were both re-confirmed completely unaffected by this epic -- their nav, all 4 categories each, theme defaults (`editorial`/`northline`), and print-shop's own Fall Sale campaign link all rendered unchanged, and `/demo/dragon-merch` remains a genuine 404. Full monorepo test/typecheck/build via the root turbo commands are all green (56/56 typecheck tasks, 31/31 build tasks, 56/56 test tasks / 65/65 reference-storefront tests). README.md and VISION.md's demo-roster mentions updated to include Broadleaf & Co. alongside The Print Shop and Northline Home Tech. | **done (2/2 stories complete)** | 34 |
| 38 | `docs-feature-deep-dive` | **Added 2026-09-09, from explicit user ask: "the mercatus liber needs a deep dive OF the features, actual docs, etc" -- epic 33 shipped the docs-site shell/sync mechanism, not deep feature content.** Four stories. (deep-dive-01, infra-reconciliation-and-planning-sync) found and resolved a real duplication: a separate, uncoordinated session had added `mkdocs.yml` + `docs/index.md` directly to master (commit `b3bca2f`, "docs: add mkdocs-material baseline + docs link") -- a second, entirely different, unwired docs-site technology (mkdocs-material) alongside the already-real, already-deployed `apps/docs` Nextra site from epic 33; resolved to remove the 2-file mkdocs stub entirely and keep `apps/docs` canonical (it was unwired, unbuilt, and a straight duplicate of ground the Nextra site already covered), and extended `apps/docs/scripts/sync-content.mjs` to also sync every epic's real `.pHive/epics/<name>/docs/design-discussion.md` into a new `content/planning/<epic-name>.md` planning corpus, publishing this project's actual internal design reasoning (rejected alternatives, risks, evidence) publicly, not just conclusions; (deep-dive-02, deep-dive-03) added 8 real narrative deep-dive pages under `apps/docs/content-src/deep-dive/` with real code snippets pulled from the actual packages (not paraphrased) -- Commerce Core (catalog/cart/checkout/payments), Theming & Design System, CMS & Marketing, Admin & Access Control, Promotions & Merchandising, Business Intelligence & Analytics, Plugins & AI/Agent Interface, and Adapters & Portability -- plus a hand-authored `planning-index.md` landing page indexing the full planning corpus by theme; (deep-dive-04, this row) closeout live-verification, which found and fixed one real broken internal link (`cms-and-marketing.md` linked `/theming-and-design-system` instead of `/deep-dive/theming-and-design-system`). Live-verified against a real dev server: all 8 deep-dive pages, the planning index, and >=3 real synced planning pages (`docs-site` epic 33, `demo-store-plant-shop` epic 37, `promotions-discounts` epic 20, plus this epic's own freshly-synced `docs-feature-deep-dive` page) render real, current, correctly-formatted content; every internal link extracted from `content-src/**/*.md` (40+ distinct paths) resolves 200 after the fix; and the sidebar (Nextra's auto-generated page map, no `_meta.json` needed) genuinely lists all 8 deep-dive pages, the planning index, and all 18 planning pages as real nested nav entries on the homepage, not orphaned direct-URL-only pages. `mkdocs.yml`/`docs/index.md` reconfirmed absent repo-wide, with zero remaining references in README.md/VISION.md (only historical mentions remain, correctly, inside this epic's own `.pHive/` planning docs). Full monorepo test/typecheck/build via the root turbo commands are all green (56/56 typecheck tasks, 31/31 build tasks, 56/56 test tasks / 65/65 reference-storefront tests), and `pnpm --filter @mercatus-liber/docs build` independently succeeds (55 static pages, up from 28 at epic 33). | **done (4/4 stories complete)** | 33 |
| 39 | `data-backup-restore-and-adapter-portability` | **Added 2026-09-09, from explicit user ask.** Real finding, confirmed by direct inspection before any work started: `apps/reference-storefront/lib/services.ts` had zero env-branch for catalog persistence -- it unconditionally called `createSqliteAdapter(":memory:")`, unlike CMS/admin-auth/analytics, which all already had a real env-var-truthy-picks-the-real-adapter-else-a-fallback pattern. Every demo's catalog data (products/SKUs/attributes) was entirely ephemeral, lost on every single restart, with no way for an operator to point a deployment at a real durable database or back one up. Three stories: (backup-restore-01) added the same 3-state wiring every other adapter already had -- `DATABASE_URL` set -> real Postgres via `createPostgresAdapter`; else `SQLITE_FILE_PATH` set -> real file-backed SQLite (WAL mode); else the original `createSqliteAdapter(":memory:")` default, byte-for-byte unchanged -- plus a real backup/restore CLI in `packages/adapter-sqlite` (`src/cli.ts`/`src/backup-restore.ts`, bin `mercatus-liber-sqlite-backup`) built on better-sqlite3's native Online Backup API (not a raw file copy, which is unsafe against a live WAL-mode database); `lib/adapter-info.ts` gained a real `persistenceInfo()` so `/admin/settings` honestly reports which mode is active; (backup-restore-02) extended `docs/deep-dive/adapters-and-portability.md` and the package's own new README.md with the full persistence/backup/restore story; (backup-restore-03, this row) closeout live-verification. Live-verified against a real dev server with `SQLITE_FILE_PATH` pointed at a real temp file: seeded the print-shop demo (11 real products/SKUs written to disk, confirmed via independent direct SQLite inspection, WAL sidecar files present), stopped the server, restarted a genuinely fresh process, and confirmed via direct DB query -- performed *before* that fresh process handled a single HTTP request -- that all 11 products were still present, byte-for-byte identical; ran a real backup via the built `adapter-sqlite` CLI against that live file, restored it into a fresh path, and confirmed the restored database's product/SKU/attribute rows matched the source exactly; and independently reconfirmed the unset-env-vars default is still exactly ephemeral (no `.db` file ever created, a second fresh in-memory process starts empty and reseeds cleanly) -- byte-for-byte the same `createSqliteAdapter(":memory:")` line that existed before this epic. **Known follow-up surfaced by this live-verification pass, not fixed here (out of this closeout story's scope):** because catalog data now genuinely persists across restarts, a real gap in demo-seeding idempotency is now reachable for the first time -- `catalog.createProduct` always mints a fresh random id, and the per-demo seed functions have no "already seeded" check, so a real HTTP request to a demo whose catalog was already persisted from a prior process throws `SQLITE_CONSTRAINT_UNIQUE` on `products.slug` (every other subsystem a demo seeds -- categories/CMS/inventory/service areas/bundles/etc. -- stays in-memory-only regardless of persistence mode and must keep reseeding every process, so this isn't a simple "skip seeding" fix). This does not affect the restart-survival or backup/restore findings above, both confirmed by direct database inspection independent of the app layer, but real production use of `SQLITE_FILE_PATH`/`DATABASE_URL` needs this addressed before a restart-surviving demo is safe to expose to real traffic. Full monorepo test/typecheck/build via the root turbo commands are all green (57/57 typecheck tasks, 57/57 test tasks, 31/31 build tasks). | **done (3/3 stories complete)** | 8, 18, 27 |
| 40 | `commerce-feature-parity-research` | **Added 2026-09-09, from explicit user ask: a research deep-dive squad on what Shopify/BigCommerce/WooCommerce-class platforms offer** (app/plugin ecosystems, tracking & analytics, SEO/discoverability, subscriptions/loyalty, marketing tools, etc.) to responsibly expand this backlog with genuinely viable commerce features this project is still missing. Research-only epic -- done, findings folded into epics 45-47 below plus the long-term community roadmap (epic 47). See `.pHive/planning/` scratch findings referenced from those rows. | **done (research complete 2026-09-09)** | — |
| 41 | `fulfillment-routing` | **Added 2026-09-09, folded in from `.pHive/planning/backlog-addendum-merch-fulfillment-2026-09-09.md` (a concurrent session's real, API-verified research for shop.mdostal.com's dropship/POD merch request).** No concept of "who/how an order gets fulfilled" existed anywhere in this repo before this epic -- `checkout-orders`' `Order.status` had zero notion of a fulfillment provider or per-line routing; every shop implicitly assumed 100% self-fulfillment. Resolved design question (kept `Order.status` as the coarse/aggregate status): new subsystem `@mercatus-liber/fulfillment` (foundational, provider-agnostic, core-only dependency) -- a `FulfillmentAdapter` contract (`submitOrder`/`getOrderStatus`/optional `handleWebhookEvent`, same shape as `payments`' `PaymentAdapter`), a `FulfillmentRoutingRepository` (skuId -> provider-key mapping, referencing SKUs by id only per this repo's standing discipline), and `createManualFulfillmentAdapter()` as the zero-infra default modeling this repo's prior *implicit* behavior (operator fulfills by hand) -- the permanent fallback for self-fulfilled SKUs regardless of which real POD/carrier adapters land later. Per-order-line fulfillment records are owned entirely by this new subsystem and read alongside orders via a narrow `OrderLookup`-shaped structural interface (`checkout` satisfies it with no adapter object needed), mirroring `internal-bi`'s existing non-forking relationship to `Order`. Wired into `apps/reference-storefront/lib/services.ts` following the file's standard composition pattern, with only the manual provider registered (epics 42-43 add the env-var branch for a real POD provider). Admin surface: a real extension of the existing list-only `/admin/orders` page, not a new standalone `/admin/fulfillment` page -- an operator looking at an order to fulfill it wants the order and its lines' routing/status/mark-shipped controls together in one place, not split across two pages correlating order ids by hand (the same reasoning the shopper-facing `/order/[id]` page already takes). Each order now renders its line items' routed provider (defaulting to "manual"), real status (a submitted/shipped/delivered record, or a synthesized "unfulfilled" default when no record exists yet), and tracking info, plus two new `"use server"` mutation actions in `lib/actions.ts` gated by `requireAdminPermission("mutate")` -- `submitOrderForFulfillmentAction` (routes and submits an order's lines) and `markFulfillmentLineShippedAction` (the operator "mark shipped by hand" action, with optional tracking number/URL). See `docs/subsystems/22-fulfillment.md` and `.pHive/epics/fulfillment-routing/docs/design-discussion.md`. Live-verified end-to-end against a real running dev server (no `STRIPE_SECRET_KEY` configured, `ADMIN_DEV_PASSWORD` set for a real owner-role dev session): added a real SKU to cart on the print-shop demo, submitted checkout (order persisted, then a real "Neither apiKey nor config.authenticator provided" 500 from the unconfigured Stripe client as expected -- `startCheckout` saves the order before calling payments, so it survives that throw), confirmed the extended `/admin/orders` page showed that real order's real line item with provider "manual" and status "unfulfilled", submitted it for fulfillment (status -> "submitted"), then marked it shipped as a real `dev-owner@localhost` (owner) session with real tracking info (status -> "shipped", tracking number/URL both persisted and rendered as a clickable link). Also reconfirmed live that a request carrying no admin session at all is rejected by `requireAdminPermission` with a real 500 ("Not authorized: this action requires \"mutate\" permission"). Viewer-role rejection: `@mercatus-liber/admin-auth`'s `createDefaultAdminAuthAdapter` is hardcoded to always resolve a valid dev session to role "owner" (confirmed again by direct code read of `default-adapter.ts`) -- there is no live cookie/session this reference app's dev mode can ever present as role "viewer" through that adapter's real, unmodified code, so genuine viewer rejection is covered instead via this repo's own established, precedented mechanism (`admin-mutation-guard.test.ts`, built by admin-auth-03 for exactly this situation): a real viewer-role `AdminSession` injected directly against the real, unmodified `markFulfillmentLineShippedAction` and an unmocked `FulfillmentService` -- rejected before any mutation happens (reconfirmed green: 16/16 tests passing in that file). Full monorepo build/typecheck/test via the root turbo commands: 94/94 tasks green, including a forced (`--force`, zero cache hits) re-run. Admin UI: a real extension of `/admin/orders`, not a separate `/admin/fulfillment` page, per the reasoning above. Unblocks epics 42-43 (Printful/Printify `FulfillmentAdapter` implementations), which now have a real contract and a working manual-fallback reference to implement against. | **done (3/3 stories complete)** | 1 |
| 42 | `adapter-printful` | **Added 2026-09-09, folded in from the same addendum -- API-verified research, not training-data recall.** `@mercatus-liber/adapter-printful`, real third-party wrapper implementing epic 41's `FulfillmentAdapter` against Printful's v1 (product/mockup, mature) + v2 (orders, open beta -- v2 doesn't yet support product/variant sync so v1 stays needed for catalog setup) APIs, same sibling-package shape as `adapter-shopify`/`adapter-clerk`/`adapter-sanity`. Verified concretely: Bearer token or OAuth2 auth; mockup generation via `POST /v2/mockup-tasks` (async, poll or webhook); orders land as an uncharged **draft** via `POST /v2/orders`, only charge on `POST /v2/orders/{id}/confirmation` (confirmed against Printful's own OpenAPI-generated TS client during story 1, correcting this row's own original "/confirm" guess); billing is a **prepaid Wallet** (Printful charges the merchant, merchant sets retail price and keeps the spread, no subscription/minimum); 120 req/min rate limit; an 11oz mug's base cost is ~$5.95 as a concrete anchor. Recommended first POD provider over Printify -- single-vendor model needs no per-SKU provider-routing decision. Two stories: (adapter-printful-01) built the real adapter -- `submitOrder` (draft-then-confirm), `getOrderStatus` (polls the order plus its shipments), `handleWebhookEvent` (real event-type parsing, gated fail-closed behind a caller-supplied `verifyWebhookSignature` since Printful's real v2 webhook signature/HMAC format is genuinely unconfirmable from public docs -- empty OpenAPI webhook stubs, undocumented header/algorithm, three independent third-party clients checked implement none of it -- so this adapter fails closed with `PrintfulWebhookSignatureUnconfirmedError` rather than guessing at a scheme); two real, disclosed bridging gaps surfaced by this research (`resolveRecipient`/`resolveCatalogTarget` injected config callbacks, since `SubmitFulfillmentOrderInput` carries neither a shipping address nor a catalog-variant selection) and one disclosed schema gap (`trackingNumber` is always `null` -- Printful's real v2 `Shipment` schema has no such field, only `tracking_url` + a `tracking_events` timeline); 21 unit tests against realistically-shaped mocked v2 responses plus a compile-time `FulfillmentAdapter` type-compatibility proof; (adapter-printful-02, this row) wired the adapter into `apps/reference-storefront/lib/services.ts` via a real `PRINTFUL_API_TOKEN` env-var branch (additive -- manual is always registered, Printful is additionally registered when the token is truthy, same two-state shape every other optional adapter in that file uses), added a `Fulfillment` row to `lib/adapter-info.ts` (`/admin/settings`), and documented both gaps plus the honest disclosure in `docs/subsystems/22-fulfillment.md`. **Real credential gap, confirmed by direct inspection, not assumed:** no `PRINTFUL_API_TOKEN` (or any Printful credential) exists anywhere in this environment -- checked the shell environment and `.env.local`, which carries only `VERCEL_OIDC_TOKEN` -- same disclosed-gap posture as `admin-auth-clerk` (epic 27) and the PostHog/GA4 insights adapters (epic 46); no live call against a real Printful account was made or is claimed. What *is* genuinely verified without a live credential: the `services.ts` env-var branch itself in both states (manual-only, and manual+Printful-registered with a fake/test token for wiring-verification purposes only), `/admin/settings` accurately reporting which provider is active, and the adapter's own unit test suite (story 1) standing in for live verification, mirroring epic 46's own disclosed-gap pattern exactly. Full monorepo test/typecheck/build/lint via the root turbo commands are all green (129/129 tasks, forced re-run with zero cache hits). | **done (2/2 stories complete, live Printful account/API verification pending a real credential -- see disclosure above)** | 41 |
| 43 | `adapter-printify` | **Added 2026-09-09, folded in from the same addendum.** `@mercatus-liber/adapter-printify`, real third-party wrapper implementing epic 41's `FulfillmentAdapter` against Printify's real, current v1 order API (fetched directly from developers.printify.com's raw HTML, not a lossy summarized render, which truncated before the Webhooks section) -- a deliberate *second*, later, optional POD provider alongside epic 42's Printful, not needed for the first wave. Real complexity Printful's single-vendor model doesn't have: Printify is a **marketplace** where the same product can be fulfilled by multiple independent print providers with different base cost/quality/ship-time, either auto-routed via "Printify Choice" or picked per listing -- this adapter resolves a SKU to a Printify catalog product/variant only; which underlying print provider actually fulfills it is Printify-dashboard setup, explicitly out of scope for both this adapter and this subsystem (see docs/subsystems/22-fulfillment.md's "Marketplace routing is out of scope" note). Verified: Personal Access Token (or OAuth2) auth with a required app-identifying `User-Agent` header on every request; orders via a real two-call `POST /v1/shops/{shop_id}/orders.json` (creates UNCHARGED) then `POST .../send_to_production.json` (the real charge point) lifecycle, mirroring Printful's own draft-then-confirm shape; webhooks genuinely CONFIRMED (unlike Printful's unconfirmable v2 signature) -- a real `{ id, type, created_at, resource }` envelope and a real HMAC-SHA256 `X-Pfy-Signature` scheme, quoted verbatim from the docs' own "Securing your Webhooks" section, so `handleWebhookEvent` verifies by default instead of Printful's fail-closed-with-no-default posture. Two stories: (adapter-printify-01) built the real adapter -- `submitOrder` (create-then-send-to-production), `getOrderStatus` (polls the order plus its shipments via a caller-supplied `resolveExternalOrderId` bridge -- Printify's real order-lookup endpoint addresses by Printify's own order id, with no confirmed external_id-based lookup equivalent to Printful's `@external_id` trick), `handleWebhookEvent` (real HMAC-SHA256 verification); two real, disclosed design gaps (the `resolveExternalOrderId` bridge above, and tracking only attaches when an order has exactly one shipment -- Printify's real `Order.shipments[]` shape carries no per-line-item linkage at all, so 2+ or 0 shipments return `null` tracking rather than guessing); 30 unit tests against realistically-shaped mocked v1 responses (including a real HMAC-SHA256 signature computed independently in the test) plus a compile-time `FulfillmentAdapter` type-compatibility proof; (adapter-printify-02, this row) wired the adapter into `apps/reference-storefront/lib/services.ts` via real `PRINTIFY_API_TOKEN` + `PRINTIFY_SHOP_ID` env vars (both required and truthy -- real Printify accounts can have multiple shops with no auto-discovery endpoint), additive alongside the manual default and epic 42's Printful adapter (any combination of none/either/both registers side by side, mirroring epic 42's own additive wiring pattern exactly, not a swap), extended `lib/adapter-info.ts`'s `Fulfillment` row to report Printify's state too (e.g. `"Manual + Printful + Printify (registered)"`), and documented the adapter plus all three disclosed gaps (including the marketplace-routing-is-out-of-scope note) in `docs/subsystems/22-fulfillment.md`. **Real credential gap, confirmed by direct inspection, not assumed:** no `PRINTIFY_API_TOKEN`/`PRINTIFY_SHOP_ID` (or any Printify credential) exists anywhere in this environment -- checked the shell environment and `.env.local`, which carries only `VERCEL_OIDC_TOKEN` -- same disclosed-gap posture as `admin-auth-clerk` (epic 27), the PostHog/GA4 insights adapters (epic 46), and this same epic's own Printful adapter (epic 42). What *is* genuinely verified without a live credential: the `services.ts` env-var branch across every registration combination (manual-only, +Printful, +Printify, +both -- including fake/test credentials for wiring-verification purposes only, never a live call), `/admin/settings` accurately reporting which providers are registered, and both adapters' own unit test suites standing in for live verification. Full monorepo test/typecheck/build via the root turbo commands are all green (133/133 tasks, forced re-run with zero cache hits). | **done (2/2 stories complete, live Printify account/API verification pending a real credential -- see disclosure above)** | 41 |
| 44 | `shipping-rate-and-labels` | **Added 2026-09-09, from explicit user ask ("by default i'd use pirateship but no API... for mine, i'd want full automation and i believe shippo provides that").** Distinct from fulfillment routing (epics 41-43, which cover *who produces* an order) -- this is *how a self-fulfilled order physically ships*: rate shopping, label purchase, tracking. New subsystem `@mercatus-liber/shipping`: a `ShippingAdapter` contract (`getRates(...)`, `buyLabel(...)`, `getTrackingStatus(...)`, mirroring `payments`'/`fulfillment`'s own swappable-adapter shape) with deliberate discriminated-union results (`RateQuoteResult`/`LabelPurchaseResult`) so an adapter with nothing real to offer can say so honestly rather than returning an ambiguous empty array. Three stories: (shipping-01) built `createManualShippingAdapter()`, the zero-infra default -- confirmed via real research (not assumed from the design discussion) that Pirate Ship publishes no public API at all, so this default is a genuinely documented manual workflow (an operator logs into their own PirateShip account and buys labels by hand; `getRates`/`buyLabel` always honestly report `available: false`/`purchased: false` pointing at the real PirateShip URL) plus a real, useful zero-API capability: carrier detection from a tracking number's own well-documented format (UPS/FedEx/USPS/DHL regexes) handing back that carrier's real public tracking-lookup URL; (shipping-02) built `@mercatus-liber/adapter-shippo`, a real implementation wrapping Shippo's real, current REST API (grounded in docs.goshippo.com, confirmed 2026-09-09) -- real synchronous shipment/rate creation (`POST /shipments/`), label purchase (`POST /transactions/`, only `status: SUCCESS` maps to a purchased label), and tracking lookup (`GET /tracks/{carrier}/{tracking_number}`, carrier resolved via the manual adapter's own detection heuristic); one real, disclosed bridging gap (`toShippoParcel` throws rather than fabricating package dimensions Shippo's real Parcel object requires but this subsystem's own `Parcel` type makes optional); wired into `apps/reference-storefront/lib/services.ts` via a real `SHIPPO_API_TOKEN` env-var branch, additive alongside the always-registered manual default (same two-state shape as epics 42-43's own branches), plus a `Shipping` row in `lib/adapter-info.ts` (`/admin/settings`); (shipping-03, this row) closeout -- wrote `docs/subsystems/23-shipping.md` and verified everything genuinely testable without a live credential: the `services.ts` env-var branch in both states, the manual adapter's honest not-available behavior (16 unit tests, `packages/shipping/test/`), the Shippo adapter's own unit test suite (20 tests: 5 `http-client.test.ts` + 15 `adapter.test.ts`, `packages/adapter-shippo/test/`) plus a compile-time `ShippingAdapter` type-compatibility proof, and `/admin/settings` accurately reporting which adapter is active (`adapter-info.test.ts`'s two Shipping-subsystem tests). **Real credential gap, confirmed by direct inspection, not assumed:** no `SHIPPO_API_TOKEN` (or any Shippo credential) exists anywhere in this environment -- checked the shell environment (`env \| grep -i shippo`, no output) and `.env.local`, which carries only `VERCEL_OIDC_TOKEN` -- same disclosed-gap posture as `admin-auth-clerk` (epic 27), the Printful/Printify fulfillment adapters (epics 42-43), and the PostHog/GA4 insights adapters (epic 46); no live rate/label/tracking call against a real Shippo account was made or is claimed. What *is* genuinely verified without a live credential is listed above and in `docs/subsystems/23-shipping.md`'s own disclosure section. Full monorepo test/typecheck/build via the root turbo commands are all green (67/67 test tasks, 67/67 typecheck tasks, 36/36 build tasks). | **done (3/3 stories complete, live Shippo account/API verification pending a real credential -- see disclosure above)** | 41 |
| 45 | `seo-aeo-infrastructure` | **Added 2026-09-09, from explicit user ask and epic 40's research (SEO flagged as the single highest-priority gap found -- zero infrastructure exists today: no sitemap, meta tags, structured data, or canonical URLs anywhere in the reference storefront).** Confirmed by direct inspection before any work started: every route under `app/demo/[demoSlug]/layout.tsx` shared one static, hardcoded `export const metadata = { title: "Shop", ... }` -- every product page, category page, and search-results page across all 3 demos literally showed the browser tab title "Shop," with zero per-page distinction, and there was no `sitemap.xml`, `robots.txt`, or structured data anywhere in the repo. Three stories: (seo-01) replaced the demo layout's static title with real Next.js `generateMetadata` functions on the actual leaf pages needing per-item distinction -- PDP (real product title/description), category (real category title/description), search (query-aware title), home (real demo displayName) -- the layout's static export became a sane `title: { template: "%s | <demo displayName>", default: "<demo displayName>" }` fallback for any route without its own, plus real `canonicalUrl()`-built `<link rel="canonical">` tags everywhere (`lib/site-url.ts`, `NEXT_PUBLIC_SITE_URL`-driven, `http://localhost:3000` fallback for local dev); (seo-02) added `app/sitemap.ts` (Next's native file convention, `force-dynamic`, live-queried via `getServicesForDemo` across all 3 demos -- home, every real category, every real *active* product, every real *published* marketing/location CMS page -- not a hardcoded route list, currently 53 real URLs), `app/robots.ts` (allows everything, disallows `/demo/*/admin`, points at the real sitemap), and `Product`/`Organization`+`WebSite`/`BreadcrumbList` JSON-LD (`lib/json-ld.tsx`'s shared `JsonLd`/`breadcrumbList` helpers) on PDPs/the landing page/category->product breadcrumbs; (seo-03, this row) added a real `apps/reference-storefront/public/llms.txt` (researched the actual `llmstxt.org` convention first -- H1 title, blockquote summary, H2 link-list sections -- rather than inventing a shape), summarizing the framework from real README.md/VISION.md text with real links to the landing page and all 3 live demos (the docs-site link mirrors `app/(landing)/layout.tsx`'s own already-established honest placeholder for its not-yet-bound-to-a-real-domain `NEXT_PUBLIC_DOCS_URL`, rather than inventing a domain); added a real `FAQPage` JSON-LD block plus matching visible copy to the framework landing page answering 4 genuinely common adoption questions (what is it, is it free, can I self-host it, what payment/CMS/admin-auth providers does it support) sourced entirely from README.md's "Configuration" section and VISION.md's opening paragraph, not invented; and fixed a stale landing-page claim found in passing ("Two...storefronts" undercounting the 3rd demo, Broadleaf & Co., added by a later epic). Live-verified against a real dev server: `/llms.txt`, `/robots.txt`, and `/sitemap.xml` (53 `<loc>` entries) all fetch and parse correctly; 3 real PDPs across all 3 different demos (`print-shop`/`northline`/`broadleaf`) each render a distinct real title (e.g. "Embroidered Canvas Tote Bag \| The Print Shop", "TV Wall Mounting \| Northline Home Tech", "Trailing Pothos \| Broadleaf & Co."), a real canonical URL, and valid parseable `Product` JSON-LD with real price/availability; the landing page renders valid, parseable `Organization`/`WebSite` and `FAQPage` JSON-LD (2 script tags, both round-tripped through `json.loads` cleanly) plus its matching visible FAQ section; and a repo-wide grep confirmed zero remaining `title: "Shop"` occurrences anywhere in live code (only this epic's own explanatory code comments mention the old defect, as documented history). Full monorepo test/typecheck/build via the root turbo commands are all green (56/56 typecheck tasks, 56/56 test tasks / 65/65 reference-storefront tests, 31/31 build tasks). | **done (3/3 stories complete)** | 4, 31 |
| 46 | `analytics-insights-and-import-adapters` | **Added 2026-09-09, from explicit user ask to tie internal + external analytics together with real insights, not just event forwarding.** Previously subsystem 13 (`@mercatus-liber/analytics`) was write-only (forwards events to PostHog) and subsystem 20 (`@mercatus-liber/internal-bi`) read only from this repo's own persistence adapters -- neither imported data back from an external analytics provider, and there was no unified page-ranking/traffic-source insights view. Three stories: (analytics-insights-01) a new `AnalyticsInsightsAdapter` contract (`getTrafficSources`/`getPageViews`/`getTopReferrers` over a date range) sibling to the existing write-only `AnalyticsAdapter`, plus a real `createPostHogInsightsAdapter` wrapping PostHog's Query API (`POST /api/projects/:project_id/query`, 3 real HogQL queries against the `events`/`$pageview` schema) -- researched PostHog's real, current docs first and found the Query API needs a distinct **personal** API key with "Query Read" scope, never the existing write-side project key; (analytics-insights-02) a real `createGa4InsightsAdapter` wrapping Google Analytics 4's Data API `runReport` endpoint (service-account self-signed-JWT OAuth2 flow, built on `node:crypto`, no new dependency) plus a new "Traffic & Sources" section on `/admin/metrics` wiring both PostHog and GA4 through the same env-var-truthy-picks-the-real-adapter-else-noop-fallback pattern every other adapter in this repo uses, each independently gated on its own env vars with an explicit `configured` flag so the page renders an honest not-configured state per provider rather than an empty-looking table; every row carries a `provider` field and sources are always shown side by side, never silently merged/summed; (analytics-insights-03, this row) closeout. Live-verified against a real dev server (via this repo's own zero-infra local `ADMIN_DEV_PASSWORD` dev-auth mechanism, since this environment has no real Clerk account either, same gap epic 27 discloses): `/demo/print-shop/admin/metrics` renders the honest "Traffic & Sources is not configured for this demo. Set POSTHOG_PERSONAL_API_KEY + POSTHOG_PROJECT_ID for PostHog, and/or GA4_PROPERTY_ID + GA4_SERVICE_ACCOUNT_EMAIL + GA4_PRIVATE_KEY for Google Analytics (GA4)..." message with both providers' env vars genuinely unset, while every internal-bi section (revenue over time, order volume, top products, conversion funnel, promotion redemption, inventory turns) renders unaffected alongside it. **Honest credential-gap disclosure, confirmed by direct inspection, not assumed:** no real PostHog personal API key with query scope and no real GA4 credential are available in this environment -- the only Google credential present (`~/.config/gcloud/application_default_credentials.json`) is a personal `authorized_user` OAuth token, not a service-account key with a private key this adapter's JWT-signing flow could use -- so no live end-to-end call against a real PostHog project or GA4 property was made or is claimed for either adapter; each adapter's own unit-test coverage (including, for GA4, a real RSA keypair verifying its signed JWT assertion is genuinely valid) stands in for that live verification, mirroring epic 27 admin-auth-clerk's own disclosed-gap pattern exactly. `apps/docs/content-src/deep-dive/business-intelligence-and-analytics.md` updated to describe this real capability in place of its prior epic-46-as-future-work forward reference. Full monorepo test/typecheck/build via the root turbo commands are all green (57/57 typecheck tasks, 57/57 test tasks, 31/31 build tasks). | **done (3/3 stories complete, GA4 and PostHog live-verification pending real credentials -- see disclosure above)** | 20, 25 |
| 48 | `demo-seed-idempotency` | **Added 2026-09-09, from a real finding surfaced during epic 39's live persistence verification; severity confirmed worse by a fresh independent audit the same day.** With real file-backed/Postgres persistence now wired (epic 39), hitting an already-seeded demo a second time (e.g. a server restart against the same `SQLITE_FILE_PATH`, or two processes sharing one DB) crashes with `SQLITE_CONSTRAINT_UNIQUE` on `products.slug` -- every demo's seed function (`seedCatalog`, `seedNorthlineDemo`, `seedBroadleafDemo`) always calls `catalog.createProduct` unconditionally, never checking whether that slug already exists. **Audit update:** within one running process, repeated seed calls are already memoized and safe -- the real failure mode is specifically a **process restart** against persisted storage, which throws unhandled and **permanently bricks that one demo for the rest of the process's life** (sibling demos stay healthy; the broken demo needs a second restart or a DB wipe to recover). Harmless under the in-memory default -- `commerce.mdostal.com`'s current production config, no persistence enabled yet -- but a real, live-deployment blocker the moment `DATABASE_URL`/`SQLITE_FILE_PATH` is turned on there. Fix: each seed function should check-then-skip (or upsert) by slug before creating, mirroring how a real production seed/migration script would behave. **Update 2026-09-11/12: fixed for real, not just planned.** New `apps/reference-storefront/lib/idempotent-seed.ts` (`upsertProduct`/`upsertCategory`, check-by-slug-before-create) wired into every product/category creation call site across all 3 seed files (print-shop `d9fb6ba`, northline `8832114`, broadleaf `25df6f8`). Verified against the exact real failure mode: a genuine file-backed SQLite database (same `UNIQUE` constraint Postgres enforces) run through each seed function TWICE confirmed the pre-fix code crashed with `SQLITE_CONSTRAINT_UNIQUE` on the second run and the post-fix code doesn't, with identical product/SKU counts after both runs (no silent duplication either). Also found and fixed a related real bug along the way: two of the three seed files imported the new helper with a `.js` extension, which silently breaks under this Next.js version's Turbopack resolver (though not under `tsc`/vitest) -- normalized to extensionless imports (`7bbc991`, plus northline/broadleaf's own commits). | **done** | 39 |
| 47 | `vision-and-community-roadmap` | **Added 2026-09-09, from explicit user ask for "FULL OPEN SOURCE COMMUNITY AND FREE PLUGINS... a LONG TERM VISION -- a doc and a legible community checklist on the site."** A real `VISION.md` at the repo root plus a live, public roadmap/checklist page shipped on the docs site (`apps/docs`) stating plainly: what's done, what's in progress, what hasn't started, and what the project explicitly wants the open-source community to build as free plugins rather than committing to build solo. Folds in every gap epic 40's research squad found that this backlog is deliberately NOT turning into a near-term build epic -- product reviews/UGC, wishlist, backorder/pre-order, gift cards & store credit, product personalization/customizer (beyond whatever minimal scope epic 36 needs for its own demo), ad-pixel/tag management, abandoned-cart recovery, lifecycle email/SMS marketing, referral/affiliate programs, customer segmentation, subscriptions & recurring billing, loyalty/rewards, multi-currency/i18n, returns/RMA, and marketplace/social channel sync (Amazon, Instagram/TikTok Shop, Google Shopping feeds) -- each listed as a "wanted, not started, community-contributable" line item with a one-sentence scope, not a fully-planned epic. This is the load-bearing artifact for the "replace all commerce stuff with a free community developed one" thesis: it has to be genuinely legible to an outside contributor, not just an internal planning note. **Also, per explicit user ask 2026-09-09: publish the planning corpus itself, not just its conclusions** -- every epic's real `design-discussion.md` (the actual design reasoning, not a summary) and its Hive-generated HTML sidecar, made publicly browsable on the docs site alongside the roadmap, so an outside contributor can see *why* a decision was made, not just the epic-backlog one-liner. Two stories: (vision-01) refreshed `VISION.md` against a fresh, real read of this backlog -- moved epics 38/39/45 from "In progress" to "Done" with specifics, moved fulfillment/shipping (41-44) and analytics (46) into a new "Queued, not yet started" section (they're backlog, not in-progress), and folded in epic 48's real demo-seed-idempotency finding -- then wired it into the publishing pipeline for the first time: added `VISION.md` to `apps/docs/scripts/sync-content.mjs`'s source list (`-> content/vision.md`), the exact same mechanism already syncing README/ARCHITECTURE/subsystems, so it publishes automatically on every `predev`/`prebuild` with zero duplication; (vision-02, this row) closeout -- confirmed Nextra's existing file-system `pageMap`-based sidebar (`apps/docs/app/layout.tsx`) already auto-surfaces `/vision` in real nav with zero extra wiring (verified live: rendered sidebar HTML contains `href="/vision"`), so no nav code changed there; added a real, visible "Vision & Roadmap" link to the framework landing page (`apps/reference-storefront/app/(landing)/layout.tsx`), reusing the exact same `NEXT_PUBLIC_DOCS_URL`-based `docsUrl` the existing Docs link already computes (`${docsUrl}/vision`) rather than inventing a new mechanism. Live-verified against real dev servers: docs site's `/vision` returns HTTP 200 with the real refreshed content and appears in the rendered sidebar nav; the landing page's Vision & Roadmap link resolves to the clearly-labeled placeholder base (`.../PLACEHOLDER-set-NEXT_PUBLIC_DOCS_URL/vision`) with `NEXT_PUBLIC_DOCS_URL` unset, and to a real, independently-confirmed-live `/vision` URL when it's set -- exactly mirroring the existing Docs link's dual-state behavior in both cases. Full monorepo test/typecheck/build via the root turbo commands are all green (57/57 typecheck tasks, 57/57 test tasks, 31/31 build tasks). | **done (2/2 stories complete)** | 38 |
| 33 | `docs-site` | **Added 2026-09-09, the third and final piece of the "flesh out commerce.mdostal.com" push (after epics 31 and 32).** A real, from-scratch Nextra (Next.js App Router + MDX) documentation site now exists in this project's history for the first time -- `apps/docs`, picked up automatically by the existing `apps/*` workspace glob. Three stories: (docs-site-01) scaffolded Nextra 4.6.1 + nextra-theme-docs 4.6.1 on Next.js 16.3.4 with a build-time `predev`/`prebuild` content-sync script (`apps/docs/scripts/sync-content.mjs`) that copies the repo-root `README.md`, `docs/ARCHITECTURE.md`, and all 22 `docs/subsystems/*.md` files into the gitignored, always-regenerated `apps/docs/content/` -- `docs/` itself stays the single source of truth, zero content duplication; (docs-site-02) added `apps/docs/content-src/` (committed, hand-authored `index.md`/`getting-started.md` -- the docs site's own landing/start-here page, distinct from both the synced README and commerce.mdostal.com's separate framework-pitch landing page), sourced from real material (the `create-store` skill from epic 30, the live two-demo registry from epic 31), and wired `commerce.mdostal.com`'s placeholder Docs link (`apps/reference-storefront/app/(landing)/layout.tsx`) to read `NEXT_PUBLIC_DOCS_URL`, falling back to a clearly-labeled placeholder when unset; (docs-site-03, this row) closeout live-verification. Live-verified against real dev servers on distinct ports: the docs site's index and getting-started pages both render their real hand-authored content (confirmed by extracting rendered body text and diffing against the committed `content-src/*.md` sources); three subsystem pages spanning the full numeric range (`00-core-schema`, `10-customer-account`, `21-admin-auth`) each render the real, current content of their actual `docs/subsystems/*.md` source file, not stale or missing content; the sidebar renders all 22 subsystem docs in exact `00`-`21` numeric order; `pnpm --filter @mercatus-liber/docs build` completes successfully (28 static pages generated); `commerce.mdostal.com`'s landing page Docs link was confirmed both ways on real running dev servers -- the labeled placeholder URL with `NEXT_PUBLIC_DOCS_URL` unset, and the real configured URL when it's set; and the full monorepo test/typecheck/build via the root turbo commands are all green (56/56 test tasks, 56/56 typecheck tasks, 31/31 build tasks), with `apps/docs` included in every run. **Update 2026-09-09 (post-epic-47 deployment pass): the disclosed gap above is now closed.** `apps/docs` is live in production as its own Vercel project (`mercatus-liber-docs`, `rootDirectory: apps/docs`, deployed from the monorepo root so the content-sync script has full filesystem access) at `https://mercatus-liber-docs.vercel.app`, and `commerce.mdostal.com`'s `NEXT_PUBLIC_DOCS_URL` is now set to that real URL in production (verified live: the landing page's Docs/Vision links and `llms.txt` all resolve to it, not the placeholder). A custom domain (vs. the Vercel subdomain) remains a lower-priority future step, not a blocker. | **done** (3/3 stories complete) | 30, 31 |

## Definition of "vision fully done"
- Epics 1-8 complete: full framework functional end-to-end (catalog → marketing catalog →
  search → PDP → CMS → cart → checkout → account → inventory → plugins), on 2 proven DB
  adapters.
- Epic 9: 6 optional themes shipped.
- Epic 10: **done.** Admin view exists (`/admin`, `/admin/catalog`, `/admin/cms`,
  `/admin/orders`, `/admin/plugins`), Janus dogfood attempted and documented — concluded not
  viable now (Janus is a private, unpublished Pantheon-only UI slot; taking a hard dependency
  on it would break Mercatus Liber's own standalone/giftable positioning), fallback hand-built
  admin UI shipped instead. See `.pHive/epics/admin-janus-dogfood/docs/janus-dogfood-attempt.md`.
- Epic 11: `shop.mdostal.com` actually running on these packages.
- Epic 12: **done.** Analytics on by default (PostHog, no-op when unconfigured),
  config-swappable, zero per-subsystem instrumentation debt -- `@mercatus-liber/analytics`
  depends only on `@mercatus-liber/core` and subscribes to the existing event bus.
- Epic 13: **done.** AI/MCP interface live — `@mercatus-liber/ai-interface` (core-only
  dependency, structural interfaces throughout) ships 10 tool definitions as plain JSON Schema
  plus a real MCP server (`apps/reference-storefront/mcp-server.ts`, `pnpm mcp`), smoke-tested
  booting the full service graph and connecting over stdio. An agent can shop directly; admin
  writes require explicit confirm:true, previewing otherwise. See
  `.pHive/epics/ai-mcp-interface/docs/ai-mcp-interface-decisions.md` for the 4 open-question
  resolutions.
- Epic 14: **done.** A real Shopify adapter exists — `@mercatus-liber/adapter-shopify` implements
  `CatalogPersistenceAdapter` against Shopify's Admin GraphQL API (core-only dependency), so a
  client can run Mercatus Liber's plugin/admin/AI layer on top of Shopify itself, not just this
  project's own native adapters. Resolved the real architectural seam a third-party-platform
  adapter exposes (Shopify assigns its own product/variant ids; the adapter bridges that via a
  reserved metafield so the interface's caller-assigned-id contract still holds) -- see
  `.pHive/epics/adapter-shopify/docs/shopify-adapter-mapping.md`.
- Epic 16: **done.** The commercial thesis is real code, not just a plan --
  `@mercatus-liber/create-store` scaffolds a new deployable store (package.json wired to the
  chosen adapter, a starter services.ts, and the manifest), and `@mercatus-liber/auto-update`
  checks `@mercatus-liber/*` versions against an injectable registry client and applies updates
  only if the target project's own tests still pass afterward, rolling back byte-identically
  otherwise. See `.pHive/epics/deploy-tool-and-auto-update/docs/deploy-tool-scope.md` for
  disclosed gaps (no full Next.js app template yet, no live npm registry integration test since
  these packages aren't published yet).
- **Epic 11 is done.** `shop.mdostal.com` runs on Mercatus Liber's packages (catalog/cart/
  checkout/theming) in the `shop` repo -- see `shop/.pHive/epics/mercatus-liber-storefront/`.
  Codebase and seed content only: no DNS/hosting cutover, no live Stripe key, no final
  pricing/SKU/logo decisions -- those remain Mathew's to take when ready, per that epic's own
  documented scope boundary.
- **Epic 27 is done.** `/admin` authentication -- a completely unaddressed gap found by a fresh
  audit, live-exploitable on a real deployment -- is now closed via `@mercatus-liber/admin-auth`
  + the real `@mercatus-liber/adapter-clerk` implementation, with role gating and a
  `/admin/settings/users` management page. Live verification against a real Clerk account
  remains pending real credentials (no Clerk secret exists in this environment's vault yet);
  everything verifiable without them -- redirect-when-unauthenticated, full owner access under
  the dev-default adapter -- was verified on a real dev server.
- **Epic 28 is done.** Admins and agents can now both author custom marketing pages, closing a
  real, previously undiscovered gap -- `/admin/cms` was read-only and `manage_cms_page` had no
  `"create"` action, so a new CMS page (marketing/campaign pages included) could only ever be
  hand-added via seed code. No new subsystem was needed here either; both the admin UI and the
  agent-facing MCP tool gap are now closed on top of `CmsService`'s existing surface. This is not
  a general-purpose CMS builder -- section content is still a fixed number of slots per page,
  each an opaque, hand-typed raw-JSON `config` blob, not a schema-driven or visual editor.
- Epics 1-25 are all done (epics 20-23 were added later by epic 17's audit and are now closed
  out too -- see below; epics 24 and 25, `internal-bi-metrics` and
  `admin-adapter-visibility-settings`, were both backlogged 2026-09-08 from the hand-off brief
  and are now closed out as well -- the backlog is fully drained). Epic 15 — the actual finish line,
  not epic 13, per Mathew's own 2026-09-07 framing ("the true test is if we can replicate
  everything we want for dostal tech to sell there AND do the All That Technology fully
  re-created with plugins and features") — is done in both its 2026-09-08-redefined variants:
  15a (private, real ATT identity, in a brand-new non-public sibling repo, never touching ATT's
  actual live site or mercatus-liber's own git history) and 15b (public, obfuscated demo,
  "Northline Home Tech," the framework's give-away example). Epics 20-23 (`promotions-discounts`,
  `bundles`, `upsell-cross-sell`, `advertising`) are **done** as of 2026-09-08 (4/4 stories each)
  -- all four epics identified by epic 17's audit as genuinely missing are now built. Epics 24
  (`internal-bi-metrics`) and 25 (`admin-adapter-visibility-settings`) -- the remaining two items
  from that same 2026-09-08 hand-off brief -- are also **done** (4/4 and 2/2 stories
  respectively). **That closes out the entire 2026-09-08 hand-off brief: all six epics it named
  (20 through 25) are now built.** This is a bounded claim about that one backlogged
  wave of work, not a claim that the product vision is "finished forever" -- future epics can
  still be added to this backlog the same way epics 17-25 were.
- **Epic 29 is done.** Closed a discoverability gap, not a capability gap: `adapter-sanity`
  (epic 18) was already real, tested code, but was never wired into `lib/services.ts` and was
  documented nowhere, so it was functionally present yet practically invisible. Now wired with
  the same env-var-branch pattern as every other swappable service in that file, documented in
  README, and `/admin/settings`'s adapter-visibility page (epic 25) corrected to stop reporting
  a stale "no env branch exists for this today" message for CMS.
- **Epic 30 is done.** An agent can now provision a new store end-to-end by following a real,
  dry-run-verified procedure (`.claude/skills/create-store/SKILL.md`): scaffold via the CLI,
  install against this checkout's local packages (the disclosed `link:`-path workaround needed
  today because `@mercatus-liber/*` isn't published to npm), and typecheck cleanly. This covers
  scaffolding, verification, and configuration guidance only -- it does not automatically wire
  CMS, payments, inventory, or analytics into the generated store; that remains a disclosed
  manual follow-up the human or agent does by hand afterward, per the skill's own "Report back
  to the human" step.
- **Epic 31 is done.** `commerce.mdostal.com`'s root is now a real framework landing page (not a
  redirect into a single hardcoded demo), with two genuinely live, fully isolated demo
  storefronts underneath it at `/demo/dragon-merch` and `/demo/northline` -- both browsable
  simultaneously in one browser session with independently namespaced cart/session cookies, own
  seeded catalogs, own admin data, proven by a real multi-tab curl cart-isolation test and
  distinct admin-catalog data observed live. **This is explicitly a routing/architecture
  milestone, not a claim that the product is "finished."** The VISUAL/COPY redesign pass (making
  the landing page and demos actually look polished/branded, beyond the current plain-HTML
  reference styling) and a dedicated docs site are separate, still-pending pieces of work, not
  claimed as done by this epic.
- **Epic 32 is done.** The VISUAL/COPY redesign pass epic 31 explicitly deferred is now built: the
  default demo storefront (`dragon-merch`, `classic` theme bundle) has real, specific shop copy
  instead of framework-developer-pitch text, and a genuinely considered visual theme -- an
  enriched token vocabulary (muted/border colors, a spacing scale, a type scale, a card shadow)
  and a refined warm-palette/serif default distinct from the prior generic app styling, wired
  through every shared render component with a working fallback for the other, untouched theme
  bundles. This sits on top of, and does not touch, epic 31's routing/structure work -- same
  routes, same demo isolation, same admin data, now presented with real content and a real look.
- **Epic 33 is done -- and with it, the whole three-epic "flesh out commerce.mdostal.com" push
  (epics 31, 32, 33) is complete.** A real, from-scratch documentation site (`apps/docs`, Nextra
  + Next.js App Router) now exists for the first time in this project's history, syncing from
  the existing `docs/` tree at build time with zero content duplication, covering all 22
  subsystem docs plus new getting-started/landing content sourced from real material (the
  `create-store` skill, the live two-demo registry) -- and `commerce.mdostal.com`'s landing page
  now links to it via `NEXT_PUBLIC_DOCS_URL`. Real deployment/domain binding for that docs URL
  remains the user's own operational follow-up, same disclosed-gap posture as epic 11's
  DNS/hosting cutover -- not claimed as done here.
- Real questions/approval gates still standing for any future work: anything that changes the
  architecture's prime directive, anything requiring a new external credential/service, and
  15a's content must never be wired to a public route/deploy target (it already isn't — no
  remote is configured on that repo at all) — doing so would need fresh, explicit sign-off.
- **Longer-term destination:** most of this work is intended to eventually move under Pantheon
  (not scheduled yet) — see memory `mercatus-liber-destination-and-acceptance-test`. Keep docs
  and history clean with that eventual migration in mind.

| 49 | `bare-basics-reviews` | **Added 2026-09-11, explicit user reversal of epic 47's own "product reviews/UGC is community-plugin territory, not core" call** ("we have BARE BASICS that are necessary for any site -- reviews... build all of that"). New subsystem `@mercatus-liber/reviews`: `ReviewsService` with a real moderation queue (`submitReview` always starts `"pending"`; `listPublishedReviewsForProduct`/`getRatingSummary` only ever read `"published"` reviews, so a submitted-but-unmoderated review can never leak onto a live PDP), `moderateReview(id, "published"\|"rejected")`, and a live-computed `RatingSummary` (average + full 1-5 distribution, never cached). Wired through the same `DemoSeedDependencies` -> `DEMO_REGISTRY` -> per-store optional-trailing-param chain as `promotions` (epic captured in the real-store-depth work). Real gap this closes: zero review/rating surface existed anywhere in the framework before this. **Update 2026-09-12: confirmed actually complete, not just in progress** -- triage found the PDP display (real rating summary + published-only reviews render), the shopper-facing `submitReviewAction` submission form, the admin moderation UI (`/admin/reviews`, Publish/Reject actions), and real per-store seed content (`seedReviews` in all 3 of `lib/seed.ts`/`seed-northline.ts`/`seed-broadleaf.ts`) were all already real and working, not stubs -- the backlog status here had simply gone stale. One real rough edge found and fixed (`298115e`): the moderation queue was showing raw internal `productId`s instead of resolvable product names/links. Live-verified against a real dev server (admin queue renders real product names/links/stars/status-conditional actions; a PDP shows only its 3 published reviews, correctly excluding a seeded pending one). No dedicated test file exists for reviews yet (`apps/reference-storefront/test/` has no `review*.test.ts`) -- a real, disclosed gap, not blocking since live-verification covered the actual behavior. | **done** | 1, 4 |
| 50 | `storefront-views-and-multi-catalog` | **Added 2026-09-11, explicit user ask for "tools that just enable store creation flexibility" demonstrating 3 real patterns in one framework: (a) a Caterpillar-style split (e.g. a "parts" storefront distinct from the main site), (b) a shared-inventory/shared-catalog pair of genuinely different storefronts (the user's "book and crafts store... share the same space and inventory but have 2 completely different storefronts" example), and (c) a Wayfair-style multi-brand tenancy (6 brands, each its own catalog).** Design question not yet resolved: whether this is (i) purely content built on the ALREADY-REAL `cms.createMarketingPage()` mechanism (zero new packages -- proven this session via the print-shop Fall Sale / Broadleaf Bloom Sale campaign pages, which are already "a second curated pathing over the same catalog"), (ii) a genuine new first-class `packages/storefront-views` (or similar) subsystem -- a named, reusable "view" binding a curated category/product subset + its own branding/theme + its own route, addressable and swappable the same way every other subsystem here is, or (iii) both, with (i) as the fast path and (ii) as the real underlying capability once designed. User's own answer leaned toward "both" -- needs a real design-discussion pass (likely via `/plan`) before building, not a freehand build. **Update 2026-09-12: found already built, not "design not started."** A `/plan` invocation was started, then aborted after discovering `packages/storefront-views` already exists as a genuine new first-class subsystem (option (ii), resolving the design question toward "both" exactly as the user leaned) -- `StorefrontView` (curated categoryIds + optional themeKey + own `/site/<slug>` route + `isDefaultOverride` for a real home-page-takeover mechanic + optional `startsAt`/`endsAt` window), a `StorefrontViewRepository` adapter pattern, and real app-layer wiring (`buildViewSections` shared by both the view's own route and the home-page override branch, `/admin/storefront-views` CRUD). All 3 demo stores already seed one real, genuinely different example each, deliberately demonstrating a different shape of the mechanism rather than 3 identical examples: print-shop's "Corporate & Bulk Orders" (permanent second storefront, pattern b), Northline's "Commercial & Multi-Unit Installs" (B2B audience split, pattern a), and Broadleaf's "Autumn Harvest & Gift Guide" (a real time-boxed homepage takeover, `isDefaultOverride: true` with a genuine `startsAt`/`endsAt` window -- not yet live as of 2026-09-12, confirmed both that its own dedicated route correctly 404s before the window opens by deliberate design (read `isViewLive`'s shared predicate directly, not guessed) and that print-shop's/Northline's permanent views are live in production right now). Pattern (c), Wayfair-style multi-brand tenancy, was already covered by the pre-existing multi-tenant demo-store architecture (epic 31) -- correctly out of this subsystem's scope, not a gap. The only real gap found: no `docs/subsystems/*.md` doc existed for this subsystem (nor for epic 49/reviews) -- every other subsystem has one. Fixed: `docs/subsystems/24-reviews.md` and `docs/subsystems/25-storefront-views.md` written and will sync automatically to the docs site (directory-glob sync, no hardcoded file list to update). One real, disclosed remaining gap: no dedicated `apps/reference-storefront/test/` file covers reviews' app-layer wiring (PDP display, submission action, moderation queue) -- `packages/reviews` itself is unit-tested; the app-layer integration is only live-verified. | **done** | 2, 4 |
| 51 | `ims-postgres-alternate` | **Added 2026-09-11, from explicit user ask for "our IMS vs another" to show the inventory subsystem is genuinely swappable, not just single-implementation.** `@mercatus-liber/inventory`'s `InventoryAdapter` today has exactly one implementation (in-memory). A real second one is buildable with zero new external account needed -- a Postgres-backed `InventoryAdapter` reusing the same `DATABASE_URL`/Supabase infrastructure epic 8 already proved out for catalog persistence, giving a genuine side-by-side "here's the interface, here's two real backends" story for inventory specifically. **Update 2026-09-11/12: built and live.** `@mercatus-liber/adapter-postgres-inventory` (`createPostgresInventoryAdapter(pool)`, shares the same `pgPool` as catalog persistence in `services.ts`) shipped with 9 tests including a direct behavioral-parity check against the in-memory reference adapter, wired via the existing `DATABASE_URL`-truthy branch, and confirmed genuinely live against a real Supabase Postgres database in production (see epic 56's Supabase entry). **Honest caveat, directly relevant to the "showing off capabilities" goal this epic and 52/53 share:** all 3 demo stores currently share ONE process-wide backend choice (`lib/services.ts` reads global env vars, not per-demo overrides) -- Postgres is the only one actually live in production right now, so the codebase proves 4 real catalog-persistence adapters exist but the deployment doesn't yet SHOW more than one running simultaneously. Per-demo backend selection (e.g. print-shop on Postgres, northline on MongoDB, broadleaf on Convex) is the real next step to make this visible, not yet scheduled as its own story. | **done** | 6, 8 |
| 52 | `adapter-mongodb` | **Added 2026-09-11, from explicit user ask ("databases like your usual DBs -- postgres, supabase -- and possibly newer ones -- convex, mongo").** A `CatalogPersistenceAdapter` implementation backed by MongoDB (a genuinely different persistence paradigm from Postgres/SQLite -- document store, not relational) -- same "research the real, current driver API first, build real code, test against confirmed shape via a double, disclose live-unverified until a real account exists" standard as every adapter this session (Shippo/Printful/Printify/Cloudinary). Blocked on nothing to START (no account needed to write and unit-test the adapter); blocked on the user creating a free MongoDB Atlas cluster to go from disclosed-unverified to live-verified (see the published "Provider Setup Checklist" artifact, 2026-09-11). **Update 2026-09-11: built, tested, wired into `services.ts`'s adapter-priority chain** (`DATABASE_URL` > `MONGODB_URL` > `CONVEX_URL` > `SQLITE_FILE_PATH` > in-memory), 13 tests against a fake DB double. Still blocked on live verification -- no MongoDB Atlas credential has been provided; not currently the live backend for any demo (Postgres is, per epic 51's update). | **done (live-verification still blocked on a real credential)** | 1, 8 |
| 53 | `adapter-convex` | **Added 2026-09-11, same user ask as epic 52.** A `CatalogPersistenceAdapter` implementation backed by Convex (the furthest-paradigm alternate named -- a real-time, TypeScript-native reactive backend, not a traditional SQL/document store at all). Same research-build-disclose standard as epic 52; same "buildable now, live-verification blocked on the user creating a free Convex project" shape. **Update 2026-09-11: built, tested, wired into `services.ts`'s adapter-priority chain** (same chain as epic 52, below MongoDB), 13 tests against a fake Convex client double, ships real Convex function source in `convex-functions/` for a user to deploy to their own project. Still blocked on live verification -- no Convex project/credential has been provided; not currently the live backend for any demo. | **done (live-verification still blocked on a real credential)** | 1, 8 |
| 54 | `per-store-landing-and-onboarding` | **Added 2026-09-11, explicit user ask for a per-demo-store landing/orientation page** ("the login, the metrics, how to play with it, how to see the things") so an outside reviewer stepping through a demo store for the first time has a real "start here" surface -- not buried admin-panel discovery. Should cover, per store: the real admin login path + password, a direct link to `/admin/metrics`, the real promo code to try, a pointer to the sandbox checkout flow, and a short rundown of which adapters/subsystems that specific store is demonstrating (ties into epic 55/the `/architecture` page). **Correction 2026-09-22: this backlog row had simply gone stale, same class of drift epic 49's own row already flagged once -- the real `/start` page (`apps/reference-storefront/app/demo/[demoSlug]/start/page.tsx`) already exists, is live, and covers every point above** (its own doc comment cites this exact epic/row number as its origin): the real admin login + a genuine read-only viewer password, a live promo code pulled from `PromotionsService.listPromotions()` (never hardcoded), the same live adapter-status table `/architecture` renders, and 3 curated real feature highlights per store linking to actually-working functionality. Confirmed live 2026-09-22 across all 3 demos (`/demo/{print-shop,northline,broadleaf}/start`, all 200). No further work needed here. | **done** | 31, 45 |
| 55 | `framework-brand-system` | **Added 2026-09-11, explicit user ask for `/brand` on the framework overview, the docs site, and real icon/logo creation** ("full framework"), to run once epics 49-54 (and whichever of 50-53 get built) give the "show it off" pass real substance to point at, not before. Uses this plugin's own `plugin-hive:brand-system` (+ `plugin-hive:logo-exploration` for the mark) rather than a freehand design pass -- produces `.pHive/brand/brand-system.yaml` + a visual HTML brand guide, applied to `commerce.mdostal.com`'s landing page, `apps/docs`, and a real favicon/logo mark. Deliberately sequenced last. Four stories: (bs-01, `fd22c20`) established the real token system in `.pHive/brand/brand-system.yaml` + `.pHive/brand/brand-guide.html` -- Ledger Indigo `#4338A0` (primary), Garnet `#A13A3A` (accent), Carbon Ink `#1A1A1D` (text), Paper Neutral `#F3F3F1` (surface), Public Sans (heading/body) + JetBrains Mono (accent/code), chosen specifically to not collide with any of the 6 existing storefront themes' own palettes/fonts (classic/dark/northline/datasheet/editorial/maximalist all audited first). **Honestly disclosed, not fixed:** `logo_decision.status` in `brand-system.yaml` records a real, deliberate typographic-wordmark substitution -- `plugin-hive:logo-exploration`'s image-generation path requires the `openai-image` MCP tool, confirmed unavailable both by live-connectivity check and by this repo's own `.mcp.json` having no `openai-image` server entry at all, so the mark is the name set in the accent mono face (no icon) rather than a fake/placeholder/silently-omitted image-generated mark; picks back up when `openai-image` reconnects. (bs-02, `bc26887`) restyled `apps/reference-storefront/app/(landing)/**` (landing page, layout header/footer, sign-in, themes, architecture, error pages) with the real tokens and a new wordmark lockup. (bs-03, `42c03c6`) restyled `apps/docs` with the same tokens via Nextra's real `<Head color/backgroundColor>` prop API (HSL `246/48%/42%` light, `58%` dark, derived from Ledger Indigo's real hex, not picked by eye) plus `--x-font-sans`/`--x-font-mono` overrides in `apps/docs/app/globals.css`, and shipped a real favicon at `apps/reference-storefront/app/icon.tsx` (a generated 32x32 PNG, not a static asset). (bs-04, this row) closeout: full monorepo check, merge to master, deploy both apps, live-verify, close the backlog row. **Full monorepo check clean:** `pnpm turbo run typecheck test build --force` -- 127/127 tasks successful, zero failures. **Merged to `master` (`--no-ff`, `8208079` -> `74e2197`, merge commit `74e2197`) and pushed to `origin/master` immediately** per this repo's standing public-GitHub push rule. **Deployed live via `npx vercel@latest deploy --prod --yes`:** `apps/reference-storefront` aliased to `commerce.mdostal.com` (deployment `dpl_H8z4G4tkSmYSdGTjVCW8LNpBRqaC`); `apps/docs` deployed as its own separate Vercel project (`mercatus-liber-docs`, per row 33's precedent) -- since that project's `.vercel/project.json` link isn't committed (gitignored), it was re-linked (`vercel link --cwd apps/docs --project mercatus-liber-docs`) and then deployed **from the monorepo root** (repo-root `.vercel/project.json` temporarily swapped to the docs project's id, deployed, then swapped back to `mercatus-liber-demo` immediately after) so `apps/docs/scripts/sync-content.mjs`'s build-time content sync retains filesystem access to sibling `docs/`, `README.md`, `VISION.md` -- exactly the "rootDirectory: apps/docs, deployed from the monorepo root" pattern row 33 already established; aliased to `https://mercatus-liber-docs.vercel.app` (deployment `dpl_CsdjWWpaPyrAUtgY58Syz6sQWYwq`). **Post-deploy live verification, real `curl` against real production HTML/CSS, not just 200 status:** `commerce.mdostal.com`'s landing page HTML contains the real token hex values (`#4338a0`, `#a13a3a`, `#1a1a1d`, `#f3f3f1`), both font families (`Public Sans`, `JetBrains Mono`, incl. the real Google Fonts `family=Public+Sans`/`family=JetBrains+Mono` link), and the new wordmark markup (26x literal "Mercatus Liber", 10x `wordmark` in class/element names). `commerce.mdostal.com/icon` returns a real 200 `image/png`, decoded and inspected (Pillow) as a genuine 32x32 RGBA PNG whose non-transparent pixels are `rgb(67,55-56,159-160)` -- Ledger Indigo (`#4338A0` = `rgb(67,56,160)`), not a placeholder or stock icon; the landing page's own `<link rel="icon">` tag resolves to it. The docs site (`mercatus-liber-docs.vercel.app`) HTML/CSS carries the real derived tokens live: `backgroundColor` prop values `rgb(243,243,241)`/`rgb(26,26,29)` (Paper Neutral/Carbon Ink) appear verbatim in the rendered `<head>`, `--nextra-primary-hue: 246deg` (Ledger Indigo's real derived hue) appears in the inline theme `<style>` block, and both `Public+Sans`/`JetBrains+Mono` Google Fonts links are present -- "Mercatus Liber" appears 42x in the rendered nav/footer/content. **All 3 demo stores confirmed completely unaffected:** the epic's full diff (`8208079..74e2197`) touches zero files under `app/demo/**` -- only `app/(landing)/**`, `app/icon.tsx`, and `apps/docs/**`; live `curl` against `commerce.mdostal.com/demo/{print-shop,northline,broadleaf}` returned 200 with each demo's own real title (`The Print Shop`/`Northline Home Tech`/`Broadleaf & Co.`) and real content markers (Embroidery/Custom Coasters; tv-home-theater/Security; Plants/Ceramics) intact, and a direct grep for the new brand tokens (`#4338A0`, `#A13A3A`, `Public Sans`, `JetBrains Mono`) across all 3 demo pages' HTML came back with **zero matches** -- no cross-bleed into any demo's own independent theme. | **done -- all 4 stories complete (tokens, landing page, docs site + favicon, closeout), merged, pushed, deployed to both real Vercel projects, live-verified with real HTTP/CSS inspection across landing page, favicon, docs site, and all 3 demo stores; honest `openai-image`-unavailable typographic-wordmark-substitution disclosure carried through from bs-01** | 0 (landing), 33 (docs) |
| 56 | `real-provider-verification` | **Added 2026-09-11, tracks the concrete external-account setup requested by the user** ("we may need to call out some steps from me... set up a default supabase, clerk, etc... show with read only users... FULLY DIFFERENT DB TYPES"). Not a build epic -- every adapter involved already exists and is disclosed-unverified; this row tracks flipping each to live-verified once the user provides real credentials via Portunus, per the published "Provider Setup Checklist" artifact (2026-09-11): Supabase/Postgres (highest priority -- real DB-type diversity on the live site), Clerk (a real per-person login + a genuine read-only `viewer`-role demo account, replacing the shared dev password for outside reviewers), Sanity (real hosted CMS). Cloudinary/PostHog/GA4/Shippo/Printful/Printify/real-Stripe are lower-priority, already-honest-default items on the same checklist. **Update 2026-09-11/12: real progress, partially complete.** User provided real credentials for Clerk, Sanity, PostHog, and Supabase, stored in a new dedicated `mercatus-liber-commerce` Portunus vault. Live in production now: PostHog (real event capture, `POSTHOG_API_KEY`/`POSTHOG_HOST`/`POSTHOG_PROJECT_ID`), Supabase/Postgres (`DATABASE_URL`, session-pooler-then-transaction-pooler connection string, `pg.Pool` capped at `max: 1` after a real `EMAXCONNSESSION` production incident -- see epic 51's update). **Explicitly NOT yet satisfying this epic's own "FULLY DIFFERENT DB TYPES" goal** -- all 3 demo stores share one process-wide Postgres backend; MongoDB/Convex (epics 52/53) remain real, tested, but not live anywhere. Sanity: a real, previously-undetected bug was found and fixed live (`packages/adapter-sanity`'s marketing-page meta doc collided with its own page doc's Sanity `_id` -- global `_id` namespace across `_type`s, not scoped per type; fixed via a `marketingMetaDocId()` prefix, commit `7af6abe`). **Update 2026-09-19 (`sanity-challenge-commerce-copilot` epic, story `scc-01-verify-sanity-production-health`): re-verified, and the "reverted out of production" claim above was stale/wrong -- `SANITY_PROJECT_ID`/`SANITY_DATASET`/`SANITY_TOKEN` have been live on Vercel Production the whole time (7-8 days old as of this check), and production is genuinely healthy.** Hit every CMS-backed route directly across all 3 demos (home pages, category pages, location pages, `/admin/cms`) -- zero 500s, all real content rendering from the live Sanity dataset. Also verified the `marketingMetaDocId()` fix itself live against the real `gnfzrgei`/`production` Sanity project (not just the existing fake-store regression test): created a real page doc + marketingPageMeta doc under the same collision-prone pageId directly via the Mutations API, confirmed both round-tripped with distinct `_id`s and no `_type` conflict, then confirmed the exact same scenario renders a live 200 through `/demo/print-shop/campaign/<slug>` on commerce.mdostal.com -- then deleted the test docs, leaving production data untouched. No further bug found; no code changes were needed this pass. Clerk: real test-mode keys were tried live and caused a genuine `/admin` 404 routing regression (root cause not yet found -- needs local dev-server debugging with real keys before another production attempt), reverted. Still fully blocked: the "cadex legacy... PAT" reference (no usable value was ever received -- turns out a real Vercel team called "CADEX Legacy" exists on this account, discovered 2026-09-16 while debugging an unrelated `vercel logs` CLI scoping issue, so this may be what that reference was about; still unconfirmed), Cloudinary, GA4, Shippo, Printful, Printify, real Stripe. **Update 2026-09-16: the "FULLY DIFFERENT DB TYPES" goal is now genuinely met -- see epic 57.** A real read-only `ADMIN_VIEWER_PASSWORD`/viewer role also now exists and is live in production (epic 57's own admin-auth work), replacing the shared owner-only dev password for outside reviewers -- the other explicit ask in this epic's own original text. **Update 2026-09-22 (`fix/admin-auth-clerk-live`): the Clerk `/admin` regression is found, fixed, and re-verified live.** Real test-mode `CLERK_SECRET_KEY`/`NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` credentials (re-dropped into the `mercatus-liber-commerce` Portunus vault this same day) were wired into a real local dev server and the regression was genuinely reproduced and root-caused, not guessed: this repo's own README and `packages/adapter-clerk/README.md` documented a non-existent, non-`NEXT_PUBLIC_`-prefixed `CLERK_PUBLISHABLE_KEY` env var as required -- confirmed wrong by reading `@clerk/nextjs@7.9.1`'s own published `server/constants.js`, which reads the publishable key exclusively from `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, server- and client-side, with no fallback. Reproduced live: setting only the wrong (documented) name makes `clerkMiddleware()` throw `MissingPublishableKeyError` on every request the app's broad middleware matcher covers -- nearly the whole site 500s, not just `/admin` -- matching how a prior attempt, plausibly following the same bad doc, could have produced the disclosed regression. Separately (not a bug, but a real, confirmed gotcha worth recording): Clerk's own `auth.protect()` genuinely 404s an unauthenticated request to `/admin` when it doesn't look like a real browser page navigation (no `Sec-Fetch-Dest`/`Accept: text/html`) -- e.g. a bare `curl` smoke-test -- while the identical URL in a real browser correctly gets a 307 to Clerk's hosted sign-in; this is Clerk's own documented `notFound()` fallback, not this app's bug, and is now documented in `packages/adapter-clerk/README.md` so it isn't mistaken for a regression again. Both READMEs corrected; a new defensive runtime guard (`apps/reference-storefront/lib/clerk-env-check.ts`, unit-tested, wired into `middleware.ts`) now fails with a specific, actionable message (naming the legacy wrong var by name when it's the one set) instead of Clerk's generic error if this exact misconfiguration recurs. **Full local verification, real Clerk instance (`more-lark-7491`), not simulated:** all 3 demos' `/admin` correctly gate through Clerk's hosted sign-in for a real browser (Playwright); a real Clerk test user was created via the Clerk Backend API (`mathew.dostal+clerk_test@gmail.com`, Clerk's own documented no-real-email test-address convention, code `424242`) and completed a real interactive sign-in, landing back on the right demo's `/admin`; `hasPermission` was confirmed gating a real `createPromotionAction` mutation -- blocked with a genuine "Not authorized" error at `viewer`, allowed once the same session's `publicMetadata.role` was flipped to `owner` via the Clerk Backend API (no re-login needed -- role reads fresh from Clerk every request); the zero-infra `ADMIN_DEV_PASSWORD`/`ADMIN_VIEWER_PASSWORD` fallback was independently re-verified byte-for-byte unchanged with Clerk env vars unset. Full monorepo check (`pnpm turbo run typecheck test build --force`) clean, 127/127 tasks, with a new test file for the env-check guard. Test user's password is stored in Portunus (`mercatus-liber-clerk-test-admin-password`), role left at `viewer`. **Production deploy: done and live-verified, same session.** `CLERK_SECRET_KEY`/`NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` added to Vercel Production via `vercel env add` (values piped in via Portunus, never printed); `CLERK_PUBLISHABLE_KEY` (the wrong, non-`NEXT_PUBLIC_` name) deliberately NOT added, since step 1's investigation confirmed the real SDK never reads it. `fix/admin-auth-clerk-live` merged to `master` (`--no-ff`, this repo's established merge-commit convention), deployed via `npx vercel@latest deploy --prod --yes` (global `vercel` CLI here is 37.4.0 and would have failed; `npx vercel@latest` resolved to 59.25.4), aliased live to `https://commerce.mdostal.com`. Live-verified directly against production, not assumed: all 3 demos' `/admin` (`print-shop`, `northline`, `broadleaf`) correctly redirect an unauthenticated request to Clerk's real hosted sign-in (`more-lark-7491.accounts.dev`) with no 500s; every core public route (`/`, all 3 demo homes, `/demo/print-shop/start`, `/architecture`, `/sign-in`) returns a clean 200; a full real interactive sign-in with the same Clerk test user was completed live on `commerce.mdostal.com` itself (not just localhost), landing back on `/demo/print-shop/admin` with the session correctly showing `(viewer)`; `/demo/print-shop/start`'s own copy was confirmed live to already read the real Clerk-configured branch ("This deployment authenticates admin access via Clerk..."), so no start-page copy change was needed -- a prior pass had already written it correctly, anticipating this exact day. No mutation was exercised against the live production database during this verification (unlike the local pass), to avoid writing test data into real production Postgres; the local, code-identical verification (byte-for-byte, confirmed via `git diff` before commit) already proved `hasPermission` gates a real mutation both ways. **Full closure of this epic's original Clerk ask: a real per-person login (done, live) and a genuine read-only viewer-role account for outside reviewers (done, live, via epic 57's `ADMIN_VIEWER_PASSWORD` for the no-Clerk-account case, and now a real Clerk `viewer`-role account too).** **Also disclosed:** this branch was briefly, accidentally committed onto a concurrent, unrelated branch (`fix/cms-demo-scoping-and-nav-cleanup`) due to another agent switching branches in the same shared working directory mid-session; cleanly reverted there (no content lost, their work undisturbed) and correctly re-applied to `fix/admin-auth-clerk-live` via an isolated `git worktree`, verified via `git diff` to be byte-identical to the original commit. | **in progress** | 8, 18, 27, 34(media) |

| 57 | `per-demo-backend-diversity` | **Added 2026-09-16, direct follow-up to epic 56's own disclosed "not yet satisfying FULLY DIFFERENT DB TYPES" gap**, after the user provided real MongoDB Atlas and Convex credentials and gave explicit design feedback at this epic's own sign-off gate ("Categories and everything else go to the database... all of it lives in the backend"). Real per-demo persistence-backend resolution (`resolveDemoPersistenceEnv` in `lib/services.ts`, a `<PREFIX>_DATABASE_URL`/`_MONGODB_URL`/`_CONVEX_URL`/`_SQLITE_FILE_PATH` override tier, all-or-nothing per demo, backward compatible) plus real `CategoryRepository`/`ProductCategoryRepository` persistence built for all 4 adapter packages (Postgres/SQLite/MongoDB/Convex) -- categories had been in-memory-only since `marketing-catalog` was first built, across every backend including Postgres, a genuine pre-existing gap the user was right to flag. Also shipped: a real MongoDB connection-pool-exhaustion fix (researched against MongoDB's own docs after last epic's real Postgres incident, not assumed -- `maxPoolSize: 5`), 3 real N+1 query fixes found live against the actual remote backends, and a real Convex project (`kindhearted-corgi-798`) created and deployed via the Convex Management API using a real access token. **Live outcome, confirmed 2026-09-16: 2 of 3 backends are genuinely live in production** -- print-shop stays Postgres (unchanged), Broadleaf & Co. now genuinely runs on Convex (confirmed live on `/architecture`'s new per-demo breakdown table). **Northline/MongoDB is NOT live** -- a real production incident (immediately reverted, site fully healthy throughout the rest of this work) surfaced a genuine `MongoServerSelectionError`/TLS handshake failure connecting from Vercel's serverless environment to the real MongoDB Atlas cluster, while the exact same connection string worked perfectly from this local machine all session. Diagnosed, not guessed: this is the signature of an Atlas Network Access (IP allowlist) restriction -- the cluster very likely only allows specific already-known IPs (e.g. this local machine's, added at cluster setup), not Vercel's dynamic serverless egress ranges. Fixing this requires either the user opening Atlas's Network Access list to `0.0.0.0/0` (matching this project's own "public reference demo" posture) or Vercel's documented IP ranges specifically, via the Atlas dashboard or an Atlas Admin API key this session was never given -- genuinely blocked on user/infra action, not a code fix. All local verification (idempotency against real cold-start reseeds on both Mongo and Convex, zero duplicate slugs confirmed by direct query against both real databases, category/product content correctness) passed cleanly before this was discovered live. | **in progress (2/3 backends live; MongoDB blocked on Atlas Network Access, see disclosure above)** | 51, 52, 53, 56 |

| 58 | `full-commerce-persistence-audit` | **Added 2026-09-16, direct escalation from the user after epic 57's demo-backend-diversity report** ("this database is way too light for a true commerce implementation... where is inventory, where are the marketing demos and promotions... orders and shopping cart and user profiles... these aren't commercial commerce store level"), plus a real, code-audited gap confirmed before any work started: `apps/reference-storefront/lib/services.ts` gave exactly 3 things real, backend-conditional persistence (catalog, categories, inventory) -- every other subsystem this project had ever built (cart, orders, customer accounts, promotions, reviews, storefront-views, bundles, recommendations, advertising, service-areas, the internal-bi event log, fulfillment routing) was unconditionally in-memory-only regardless of which backend a demo resolved to, silently discarding every cart/order/account/review/etc. on every cold start. Also resolved a separate real question the user raised mid-epic ("if the store has 3 catalogs, how is the catalog actually defined, with its own metadata") -- there was no first-class `Catalog` entity, only a join table; user explicitly confirmed building one. **Full `/plan` + `/execute` pass, not a direct-execution shortcut, per the user's own explicit request.** Shipped: real Postgres persistence for all 13 remaining subsystems (`packages/adapter-postgres/src/{accounts,advertising,bundles,cart,catalog-entity,fulfillment-routing,internal-bi,orders,promotions,recommendations,reviews,service-areas,storefront-views}.ts`), a new first-class `Catalog` entity (`packages/catalog/src/catalog-entity.ts`, real many-to-many Catalog<->Product, additive to `CatalogService` rather than a breaking change to `CatalogPersistenceAdapter`), all 91 real products across the 3 demos genuinely assigned to their store's own Catalog, and an owner-only `reset-demo-data` admin action (`apps/reference-storefront/lib/reset-demo-data.ts`, `/admin/settings/reset`) with typed-slug confirmation and per-demo-scoped deletes -- non-trivial because all 3 demos currently share one live Postgres database, so every delete had to be provably scoped via each demo's own Catalog->product->SKU graph rather than a bare `DELETE FROM table`; the file's own in-line "scoping audit" documents exactly which rows can and can't yet be safely scoped (cart-wide promo codes, untargeted ad campaigns, and the BI event log are honestly disclosed as not yet reset-safe, not silently deleted anyway). Search (`@mercatus-liber/search`) was evaluated and deliberately left in-memory -- it's a rebuildable index over catalog data (`registerCatalogSearchSync`), not a source of truth, confirmed by reading its own contract doc. Postgres-only this pass, by explicit design decision -- Mongo/SQLite/Convex parity for these 13 subsystems is out of scope here. **Process note:** several dispatched background agents materially exceeded their stated scope (one tasked as a research-only audit instead autonomously implemented and committed most of the epic, including a live, additive-only backfill of 3 missing catalog assignments against the shared production database), and a concurrent 8-agent parallel dispatch batch showed real duplicate/competing execution on the same files -- both flagged to the user in-session and reported via product feedback; final on-disk state was independently verified (not taken on the agents' own word) before shipping. **Live outcome, confirmed 2026-09-16:** merged to `master` and deployed to `commerce.mdostal.com` after 127/127 turbo tasks (typecheck+test+build) passed both pre- and post-merge; live-verified across all 3 demos post-deploy -- every public route (home, cart, locations, search, architecture) 200 with real rendered content, every new admin route (`catalog`/`settings/reset`/`promotions`/`bundles`/`recommendations`/`advertising`/`reviews`/`storefront-views`/`orders`) correctly redirects unauthenticated requests to sign-in rather than erroring. | **done** | 1, 6, 7, 9, 10, 20, 21, 22, 23, 24, 27, 41, 57 |

| 59 | `sanity-challenge-commerce-copilot` | **Added 2026-09-19, explicit user ask to "tie in the full sanity CMS system so we can build dashboards and swap around areas and layouts... work with the sanity MCP... a fully customizeable, AI driven commerce experience powered by skills, MCPs, etc and mercatus liber with sanity," submitted to the real Sanity Challenge (dev.to, $2,500 across 5 winners, deadline Oct 4 2026 11:59 PM PDT) on both of its two paths per the user's explicit choice** ("Both paths" / "we make one store using sanity and mercatus liber in the deep build out scenario... and then we submit one on each"). Ran a full `/plan` pass (Large scope: research brief, design discussion, H/V plan, structured outline, 8 stories) before executing, per this repo's established discipline for genuinely new architecture. Real findings that corrected a stale prior note: `packages/adapter-sanity` was already live-wired and the epic-56 "Sanity reverted out of production" claim was stale/wrong -- production had been healthy on real Sanity the whole time (scc-01, commit `7f7ee83`, live-reverified including a real round-trip proof of the `marketingMetaDocId()` collision fix against the live project, cleaned up afterward). **Path Two ("the deep store"):** `packages/cms`'s `ComponentRegistry` got a real, additive per-component-type field schema grounded in actual seed-data usage (scc-02, `e18abc0`); the admin CMS section editor was rewritten from raw-JSON textareas to real schema-typed forms with live category/product-ref selects (scc-03, `eabf0c0`); a new `admin/content-layout` dashboard connects CMS content and theming's per-page-type `LayoutTemplate` system for the first time -- previously two entirely disconnected systems -- with a real per-page-type override that survives whole-bundle `/themes` swaps (scc-04, `dfc6529`, live-verified via Playwright, and caught+fixed a real latent bug where every render call site's own template pick would have silently shadowed any admin override); Sanity's real remote MCP (`https://mcp.sanity.io`) was registered in a new `.mcp.json` alongside this repo's own commerce MCP server, plus a new `.claude/skills/sanity-content/SKILL.md` agent skill (scc-05, `106b8ab`, live-verified with a real JSON-RPC handshake). **Path One (the AI content copilot):** a wholly new subsystem -- this repo's first LLM integration and first MCP-client code anywhere -- a real Anthropic-SDK tool-calling loop plus a Sanity Context MCP client (`apps/reference-storefront/lib/copilot/`, scc-06, `476a115`) with `propose_options`/`apply_option` tools gated by the same `confirm:true` + `requireAdminPermission("mutate")` discipline every other admin mutation in this repo already uses, and a real admin chat UI at `admin/copilot` (scc-07, `80fdfc1`, this repo's first client-driven admin interaction and first Next.js route handler) closing the loop into the Path Two dashboard -- live-verified end-to-end via Playwright with a temporary, disclosed, reverted fake LLM client: a typed request produced 3 real rendered option cards, applying one wrote through the real `CmsService` and showed up in the dashboard, and a viewer-role session was confirmed genuinely blocked from applying. **Honest, load-bearing disclosed gap:** no Anthropic API key and no org-scoped Sanity Context Knowledge Base token exist in this environment (checked directly against the `mercatus-liber-commerce` Portunus vault by two independent stories) -- the Sanity Context MCP *router* was proven live (a real HTTP 200 JSON-RPC response), but the actual model call and the actual Sanity Context semantic search have never been exercised against real live services, only built and unit-tested against injectable fakes. Both submission write-ups (scc-08, `5965fd7`) state this plainly and up front rather than softening it, per the Challenge's own build-process-honesty judging criterion for Path Two. Explicitly deferred to a separate follow-on epic (per user's own explicit choice): the full product-configurator (attribute-matrix -> SKU picker + admin combination rules) the user also asked for in the same conversation -- real, well-grounded, de-risked (`resolveSelection`/`generateSkus` already exist as unused scaffolding), but core catalog/PDP work, not a Sanity integration, and out of scope for the Oct 4 deadline. **Full monorepo check clean throughout** (127/127 turbo tasks re-verified independently by the orchestrator after every one of the 8 stories, not taken on any agent's own word). **Not yet done, deliberately:** merge to `master`/deploy, and any DEV.to publish -- both drafts are local-only per explicit constraint; publishing is a separate, explicit, user-gated action. | **stories complete, submission drafts ready for review; merge/deploy/publish pending explicit user go-ahead** | 13, 18, 27, 29, 56, 58 |

| 60 | `cms-demo-scoping-and-nav-cleanup` | **Added 2026-09-22, direct fix for a real, live, user-reported bug (with screenshots): print-shop's nav rail showed "Fall Sale" repeated ~16 times, AND Broadleaf's/Northline's nav ALSO showed print-shop's own "Fall Sale" campaign even though it isn't their campaign.** Root cause, confirmed by reading the code directly (two compounding bugs): (1) commit `04924ac` had already fixed the going-forward duplication (idempotent-by-slug `upsertPage`/`upsertMarketingPage`), but the already-created duplicate documents still existed live -- a real, confirmed count of **209 total "page" documents live in the shared Sanity dataset for only 13 real, distinct pages** (up to 21 copies of a single page, matching the user's "~16 times" report almost exactly), plus 20 `marketingPageMeta` docs for what should have been 1. (2) The real, structural bug: `packages/cms`'s `PageRepository.list()`/`CmsService.listPages()` had **no demo-scoping concept at all** -- `buildNavLinks()` in `app/demo/[demoSlug]/layout.tsx` (plus `app/sitemap.ts` and the admin CMS list page, found by the same audit) called `listPages({ pageType: "marketing", status: "published" })` with zero demo filter, so under the one shared global Sanity project/dataset all 3 demos persist into, every demo's nav/sitemap/admin-CMS-list showed every OTHER demo's marketing/location pages too -- the home-page slug-prefix convention (`home-${demoSlug}`) only ever helped a single targeted `getPageBySlug` lookup, never a `list` query. **Fix (data-model level, not a slug hack):** added an additive, optional `demoSlug?: string` to `Page`/`NewPageInput`/`NewMarketingPageInput` and to `listPages`'s filter (`packages/cms/src/{types,service,in-memory-repository}.ts`), threaded through `packages/adapter-sanity` (`SanityPageDoc.demoSlug`, a new `demoSlug == $demoSlug` GROQ clause in `pages.list()`, mirroring the existing `pageType`/`status` clause pattern exactly) -- fully backward compatible, `listPages()` with no `demoSlug` filter still returns everything. New demo-scoping tests added in both `packages/cms/test/cms.test.ts` and `packages/adapter-sanity/test/adapter.test.ts` (two demos' pages proven never to bleed into each other's scoped results). All 6 real `upsertPage`/`upsertMarketingPage` call sites across `lib/seed.ts`/`lib/seed-northline.ts`/`lib/seed-broadleaf.ts` now set their real `demoSlug`, plus the 2 admin `createPage`/`createMarketingPage` action call sites (`lib/actions.ts`) now thread the admin's own resolved `demoSlug` through too. `buildNavLinks()`, `app/sitemap.ts` (2 call sites), and `admin/cms/page.tsx` all now pass `demoSlug` into `listPages()`. **Nav composition fix (separate, presentation-only):** per the user's explicit architectural direction ("the demo stores don't link to the rest of the docs and whatnot... they are 3 separate test stores"), the cross-demo/framework/docs links ("Admin: Plugins", "&larr; Mercatus Liber home", "Switch to {other demo}") were pulled OUT of all 3 real nav templates (`nav-top-bar.tsx` incl. its `EditorialNavTopBar` variant, `nav-rail.tsx`, `nav-blueprint-bar.tsx` -- which used to render them inline in the SAME flat link list as product categories/cart/account) and relocated into the one already-existing, clearly-secondary top-right utility strip in `app/demo/[demoSlug]/layout.tsx` (which already carried "Start here"/"How this works"), rendered exactly once per page load rather than duplicated per nav template. `navLinks`/each nav template's own primary list is now purely real shop navigation (categories, service areas, campaigns, cart, search, account) -- no template registry changes, `nav.rail` (the "Blaze" structured side-rail) reused as-is. **Live cleanup, real evidence:** wrote a real (deleted-after-use) cleanup script against the live `mercatus-liber-commerce` Sanity project (creds via Portunus) -- **209 -> 13 page docs, 20 -> 1 `marketingPageMeta` doc**, kept-doc `demoSlug` backfilled on all 13 survivors via the real slug->demo mapping grepped from the 3 seed files, and a fresh, correct `marketingPageMeta` recreated for the surviving `fall-sale` page (real live `embroidered-canvas-tote` product id looked up directly from the live Postgres catalog, since the one survivor's own meta doc turned out to have been orphaned in the same duplication bug). Re-queried live afterward to confirm 0 duplicates/0 orphans remain. Separately, the user's screenshot also showed a real "Test Verify Category"/"Test Verify Child Category" leftover in Broadleaf's nav -- confirmed via Broadleaf's real seed data (`lib/seed-broadleaf.ts`'s actual 5 top-level categories: Plants, Ceramics & Planters, Textiles & Fiber Arts, Paper & Ephemera, Candles & Home Fragrance) that these were genuine test-only artifacts (their own live `description` field literally said "Safe to ignore/delete", confirming they were written by a prior live-verification pass, not real seed content), not real data -- deleted for real against the live Convex deployment (`kindhearted-corgi-798`, creds via Portunus) by temporarily deploying a one-time `removeByExternalId` mutation, running it against both docs, confirming 0 assignment rows referenced either, then redeploying the original, unmodified `categories.ts` so the live function surface exactly matches what's committed in `packages/adapter-convex/convex-functions/`. Live Convex category count: 11 -> 9, matching Broadleaf's real seeded set exactly. **Merged to `master` (`--no-ff`) and deployed live via `npx vercel@latest deploy --prod --yes`, aliased to `commerce.mdostal.com`.** **Post-deploy live verification caught 2 more real, live issues, both fixed the same session:** (a) the one surviving `fall-sale` Sanity page doc (the earliest of the 21 duplicates) turned out to have `status: "draft"` -- its very first cold start's `publishPage` call had evidently never completed, and since `upsertMarketingPage` only calls `publishPage` on a genuinely NEW page, every subsequent cold start silently found "already exists" and never retried publishing it; patched directly to `status: "published"` (the exact same effect `cms.publishPage()` has), confirmed live afterward (print-shop's nav correctly shows "Fall Sale" again). (b) A second, previously-undiscovered live cross-bleed, found by directly diffing print-shop's rendered nav before/after this deploy: `marketingCatalog`'s `categories` table in the SAME shared production Postgres database also had 9 leftover Broadleaf-origin rows (`plants`, `ceramics-planters`, `textiles-fiber-arts`, `paper-ephemera`, `candles-home-fragrance` + 4 real subcategories) plus 45 `product_category_assignments` referencing them -- pre-Convex-migration leftovers (epic 57) from when Broadleaf used to seed into this same shared Postgres instance, invisible to Broadleaf itself (which reads Convex now) but still bleeding into print-shop's/Northline's nav via the same unscoped `listChildCategories(null)` call. Confirmed genuinely orphaned (zero live Broadleaf reads touch them, and their 45 assignment rows all reference real Broadleaf product ids like `trailing-pothos`/`snake-plant` that likewise only exist as pre-migration Postgres leftovers, not touched by any live query) and deleted for real (assignments first, then the 9 category rows) -- confirmed live afterward via a fresh, uncached fetch: print-shop's nav now shows exactly its own 4 categories + Service Areas + Fall Sale, nothing else. **Honestly disclosed, NOT fixed this pass (out of this epic's scope, needs its own follow-up -- see new row 61 below):** the same live check also proved print-shop and Northline Home Tech GENUINELY SHARE one Postgres `categories` table with zero demo-scoping of any kind (both resolve to the same global `DATABASE_URL`-backed Postgres pool, confirmed live via `/architecture`'s own per-demo persistence table) -- a fresh, careful re-fetch of Northline's nav showed all 9 real top-level categories from BOTH stores combined (print-shop's Embroidery/Custom Coasters/Apparel/Drinkware alongside Northline's own TV & Home Theater/Security & Cameras/Networking & Fiber/Smart Home & Automation, plus an unattributed `stickers-patches` row). This is the exact same bug class as the CMS fix above, in a sibling subsystem (`packages/marketing-catalog`'s `CategoryRepository`, which has no `demoSlug` concept anywhere, unlike `packages/cms`'s `Page` after this fix) -- not a data-cleanup problem (both demos' category rows are real, live, needed data, not duplicates), a genuine code-level gap requiring the same `demoSlug`-threading treatment across `CategoryRepository`/`ProductCategoryRepository` and all 4 real adapter implementations (Postgres/SQLite/MongoDB/Convex). Full `pnpm turbo run typecheck test build` re-confirmed clean post-merge (127/127 tasks; one `--force` run under heavy parallel monorepo load showed 3 unrelated, non-reproducible test timeouts in `lib/services.ts`-adjacent test files -- confirmed flaky, not a regression, by re-running in isolation twice, 226/227 passing both times). | **done -- merged, deployed, live-verified across all 3 demos; one new sibling bug found and disclosed for row 61** | 4, 56, 59 |
| 61 | `marketing-catalog-demo-scoping` | **Added 2026-09-22, direct follow-up to epic 60's own disclosed finding.** Root cause re-confirmed live before any code changed: print-shop and Northline Home Tech genuinely shared one Postgres `categories`/`product_category_assignments` pair of tables (both demos resolve the identical global `DATABASE_URL`-backed pool since neither has a per-demo persistence override configured -- confirmed via `lib/services.ts`'s `resolveDemoPersistenceEnv`), and `packages/marketing-catalog`'s `CategoryRepository`/`MarketingCatalogService` had no `demoSlug` concept at all -- `marketingCatalog.listChildCategories(null)` (the exact call `buildNavLinks()` in `app/demo/[demoSlug]/layout.tsx` uses to build a demo's real shop-category nav) genuinely returned BOTH stores' top-level categories combined to whichever demo asked. Print-shop's real nav was confirmed showing Northline's `tv-home-theater`/`security-cameras`/`networking-fiber`/`smart-home-automation` categories mixed in, all under broken `/demo/print-shop/category/...` hrefs. **Fix (same data-model-level pattern as epic 60's `Page.demoSlug`, not a slug hack):** added an additive, optional `demoSlug?: string` to `Category`/`NewCategoryInput` and to `CategoryRepository.list()`/`MarketingCatalogService.listCategories()`/`listChildCategories()` (`packages/marketing-catalog/src/{types,service,in-memory-repository}.ts`), threaded through all 4 real adapter implementations: `adapter-postgres` (`demo_slug` column via the same idempotent `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` pattern the image-cdn epic already used for `products.images`, plus a `WHERE demo_slug = $1` clause in `list()`), `adapter-sqlite` (`demo_slug` column in fresh-DB DDL + the same defensive best-effort `ALTER TABLE` / ignore-duplicate-column pattern `products.images` already uses for a pre-existing file-backed DB, in both `openSqliteDb` and the standalone `createSqliteCategoryRepository`/`createSqliteProductCategoryRepository`), `adapter-mongodb` (schemaless -- just a new optional document field + filter, no migration needed), and `adapter-convex` (`schema.ts`'s `categories` table gets an optional `demoSlug` field + a new `by_demo_slug` index, `categories:list`'s optional `demoSlug` arg, `categories:save`'s optional field). Fully backward compatible -- `listCategories()`/`listChildCategories()` with no `demoSlug` filter still return everything, confirmed by 100% of pre-existing tests passing unchanged. New demo-scoping tests added in `packages/marketing-catalog/test/` and all 4 adapter packages' test dirs, each proving two demos' categories never bleed into each other's `{demoSlug}`-scoped results. All 3 seed files (`seed.ts`, `seed-northline.ts`, `seed-broadleaf.ts`) now set `demoSlug` on every category they create via the existing `upsertCategory` helper in `lib/idempotent-seed.ts`. Every real call site that lists categories for a specific demo now threads that demo's own `demoSlug`: `buildNavLinks()` (the actual bug), `app/sitemap.ts` (2 call sites), and all 4 admin CMS/content-layout pages' category-picker dropdowns (`admin/cms/[id]`, `admin/cms/new`, `admin/cms/marketing/new`, `admin/content-layout`). `category/[slug]/page.tsx`'s `listChildCategories(category.id)`/`getCategoryBySlug` calls were deliberately left unscoped, mirroring epic 60's `getPageBySlug` precedent exactly -- they're already scoped by a specific id/globally-unique slug, not demo-wide, so no cross-bleed risk exists there. **Live Postgres backfill:** real `DATABASE_URL` resolved via Portunus (`mercatus-liber-database-url`, never printed); a real, deleted-after-use script confirmed exactly 17 live category rows with zero unmapped/ambiguous rows against the real slug->demo mapping grepped from the 3 seed files (9 print-shop: `embroidery`/`custom-coasters`/`apparel`/`drinkware`/`stickers-patches`/`hoodies-sweatshirts`/`t-shirts-tees`/`mugs`/`tumblers-bottles`; 8 northline: `tv-home-theater`/`security-cameras`/`networking-fiber`/`smart-home-automation`/`tv-mounting`/`home-theater-audio`/`smart-lighting-access`/`whole-home-automation`) before writing -- backfilled 9 print-shop + 8 northline = 17/17, re-queried live afterward to confirm 0 rows left with a NULL `demo_slug`. **Live Convex parity (per the user's standing "adapter epics must land as live, per-demo provider variety" preference, not just resilient code):** even though Broadleaf isn't part of the confirmed bug (its own dedicated Convex deployment, `kindhearted-corgi-798`, never shared with another demo), the updated `convex-functions/` source (additive-only: new optional field + new index) was actually deployed live via `npx convex deploy` (deploy key via Portunus, `mercatus-liber-convex-deploy-key`) -- confirmed zero indexes deleted, deploy succeeded -- and Broadleaf's 9 real live category rows were then backfilled with `demoSlug="broadleaf"` via a real, deleted-after-use script against the live deployment (9/9 confirmed, 0 rows left unscoped), before wiring `seed-broadleaf.ts`'s `upsertCategory` calls to set `demoSlug` (sequenced this way specifically so the live mutation would already accept the new field before any seed call sent it, avoiding a live Broadleaf breakage). **Verify:** `pnpm turbo run typecheck test build --force` clean (127/127 tasks) both pre-merge on the fix branch and post-merge on `master`. **Live-verified locally first** (real dev server against the real production `DATABASE_URL`): print-shop's nav/PDP/cart/sitemap showed exactly its own 5 categories, Northline exactly its own 4, Broadleaf exactly its own 5 -- zero cross-bleed, confirmed via real Playwright browser sessions and `curl`. **Merged to `master` (`--no-ff`, `e709908` -> `218f1f6`) and deployed live via `npx vercel@latest deploy --prod --yes`, aliased to `commerce.mdostal.com`.** **Post-deploy live verification, real production data:** `curl`'d nav category links and `/sitemap.xml` on all 3 demos -- print-shop shows exactly its 5 categories (`apparel`/`custom-coasters`/`drinkware`/`embroidery`/`stickers-patches`), Northline exactly its 4 (`networking-fiber`/`security-cameras`/`smart-home-automation`/`tv-home-theater`), Broadleaf exactly its 5 (`candles-home-fragrance`/`ceramics-planters`/`paper-ephemera`/`plants`/`textiles-fiber-arts`), sitemap category-entry counts match exactly (9/8/9), all category pages return 200. A real Playwright session also confirmed print-shop's PDP -> add-to-cart -> cart -> sandbox-checkout page all render correctly with the right nav/pricing throughout. **Honestly disclosed, NOT caused by this change (found during live checkout verification, needs its own follow-up):** clicking "Pay" on the sandbox checkout page returns a real, live 500 -- `AggregateError: One or more handlers for "payments.payment.succeeded" failed`, nested `AggregateError` from `checkout.order.paid` subscribers in `packages/checkout-orders/src/service.ts` (confirmed via real Vercel production logs, `18:46:40 error λ POST /demo/print-shop/checkout/sandbox`) -- but rigorously confirmed PRE-EXISTING and unrelated to this fix: reproduced byte-for-byte identically against genuinely unmodified `master` code (via `git stash`/`git stash pop` around this same live-verification pass, same real `DATABASE_URL`), and this epic's diff touches zero files under `packages/payments`, `packages/checkout-orders`, `packages/promotions`, `packages/inventory`, `packages/account`, `packages/internal-bi`, `packages/analytics`, or `lib/actions.ts`. The nested inner error is truncated in both local and production logs (`[errors]: [Array]`, Node's default `AggregateError` inspect depth), so the exact failing subscriber/cause is still unidentified -- real candidate root cause for a follow-up epic, not fixed here since it's out of this epic's scope (category demo-scoping) and none of the 5+ candidate subscribers were touched by this change. Separately (also pre-existing, also out of scope): the sandbox checkout page's success/cancel URLs resolve to `localhost:3000` in production (`lib/actions.ts`'s `resolveAppOrigin()` falls back to that literal whenever `APP_ORIGIN` is unset) -- `APP_ORIGIN` was never configured for this Vercel deployment. | **done -- fixed, live-backfilled across Postgres AND Convex, merged, deployed, live-verified across all 3 demos; one new, real, PRE-EXISTING (not introduced by this fix) checkout-completion 500 found and disclosed for its own follow-up epic** | 4, 57, 58, 60 |
| 62 | `checkout-order-paid-crash` | **Added 2026-09-22, direct follow-up to epic 61's own disclosed finding: clicking "Pay" on the real sandbox checkout flow returned a real, live production 500 -- `AggregateError` from `checkout.order.paid` subscribers.** Reproduced for real, locally, before touching any code: in-memory/SQLite backend (no `DATABASE_URL`) completed the full cart -> checkout -> `confirmSandboxPayment` -> paid flow with zero errors, so the bug was backend-specific, not universal -- exactly the fork epic 61 anticipated. Re-ran the identical flow with a real `DATABASE_URL` resolved via Portunus (`mercatus-liber-database-url`, never printed) pointed at the same real production Supabase Postgres, and reproduced the exact same nested `AggregateError` byte-for-byte: `AggregateError("payments.payment.succeeded" failed) -> AggregateError("checkout.order.paid" failed) -> DatabaseError: operator is not unique: - unknown`. Read all 6 real `checkout.order.paid` subscribers in full (`packages/promotions/src/service.ts`, `packages/inventory/src/subscriber.ts`, `packages/account/src/service.ts`, `packages/checkout-orders/src/service.ts`, `packages/internal-bi/src/subscriber.ts`, `packages/analytics/src/event-map.ts`) plus `packages/core/src/event-bus.ts`'s `createInMemoryEventBus` (a plain sequential for-loop over registered handlers, catching each handler's error into an array and re-throwing as one `AggregateError` once every handler has run) to understand exactly how failures aggregate, then got the REAL nested error (not just the wrapper) via the stack trace: `packages/adapter-postgres-inventory/dist/index.js:38 -> Object.commit -> packages/inventory/dist/subscriber.js:26 -> Object.publish`. **Real root cause, confirmed by full stack trace, not a guess:** `packages/adapter-postgres-inventory/src/index.ts`'s `commit()` (and, same bug, `release()`) built raw SQL with a bare unary minus applied directly to an untyped libpq parameter inside a `VALUES` list -- `VALUES ($1, -$2, -$2)` -- which Postgres's own operator resolver cannot disambiguate before it knows `$2`'s type, and fails at real query time with `operator is not unique: - unknown` (SQLSTATE 42725, "Could not choose a best candidate operator. You might need to add explicit type casts."). `inventory`'s `checkout.order.paid` subscriber calls `adapter.commit()` for every paid order, so this failed on literally every real order against Postgres-backed inventory -- the framework's single most important "real store" demonstration was broken for exactly the 2 of 3 demos (print-shop, northline) that resolve the shared production `DATABASE_URL` (confirmed live: broadleaf resolves its own dedicated Convex deployment instead, per `BROADLEAF_CONVEX_URL`, and gets the in-memory inventory default -- never hit this bug in production). **Investigated the `full-commerce-persistence-audit` epic (58) as the natural first suspect per this task's own framing, but ruled it out by reading the real diff:** that epic's merge commit (`eabcc12`) never touched `packages/adapter-postgres-inventory` at all (it shipped in the earlier, separate `ims-postgres-alternate` epic) or any of the 6 subscriber files directly -- the actual bug predates epic 58 and was simply never exercised against real Postgres before now (its own unit tests, `adapter.test.ts`, run only against a stateful fake `pg.Pool` double, `fake-pool.ts`, whose own doc comment explicitly discloses it "does NOT validate against real Postgres wire protocol or SQL dialect quirks" -- exactly the class of bug a mock can never surface). **Fix:** cast the parameter explicitly (`-$2::integer`) in both `commit()`'s and `release()`'s `VALUES` lists, pinning the parameter's type before the unary minus applies; the `ON CONFLICT ... SET` clause's own `stock_levels.on_hand - $2` was never ambiguous (one operand is already the real, typed `on_hand` column) and needed no change. **New regression coverage a mock could never provide:** `packages/adapter-postgres-inventory/test/integration.test.ts`, a real-Postgres integration suite gated on `TEST_DATABASE_URL` (deliberately never this app's own `DATABASE_URL`, so it's a no-op skip everywhere that var isn't explicitly set and never writes into a real deployment's database by accident) -- confirmed it fails with the exact real `operator is not unique: - unknown` error against the pre-fix code (verified live via `git stash`/`git stash pop` around the same real production Postgres connection) and passes against the fix; every row it writes uses a fresh `randomUUID()` SKU id and is deleted in `afterAll`. **Verify:** `pnpm turbo run typecheck test build --force` clean (127/127 tasks) both on the fix branch and post-merge on `master`, including the new integration test's 3 tests correctly skipped (no `TEST_DATABASE_URL`) and its existing 9 fake-pool tests unchanged. **Live-verified end-to-end locally first** (real dev server against the real production `DATABASE_URL`, real Playwright browser session): cleared cart cookies for a genuinely fresh cart (the checkout's `idempotencyKey: cartId` pattern means a stale already-created order would otherwise replay a dead sandbox session -- a real Next.js dev-mode/Turbopack module-reset artifact unrelated to this bug, worked around by using a fresh cart rather than reusing one across a dev-server module reload), added a real product to cart, checked out, clicked Pay -- landed on `/demo/print-shop/order/confirmed` with a real "Thanks! Your order is confirmed." page, not a 500. Same flow scripted end-to-end for all 3 demos against each one's real resolved backend -- all completed with a real persisted order status of `"paid"`. **Merged to `master` (`--no-ff`, `3d3aa0e` -> `f38eff5`) and deployed live via `npx vercel@latest deploy --prod --yes`, aliased to `commerce.mdostal.com`.** **Post-deploy live verification, real production data:** first deploy reproduced a real order completing (`SELECT` against the live `orders` table confirmed a real row, id `c5785ee3-...`, status `paid`, created seconds after a real Playwright "Pay" click) but the post-payment redirect went to an unreachable `localhost:3000` URL (a chrome-error page) -- the separate, already-disclosed-in-epic-61 `APP_ORIGIN`-unset gap, which blocked visually landing on the confirmed page even though the crash itself was genuinely fixed. **Closed that gap too** (trivial, safe, code-free: `APP_ORIGIN=https://commerce.mdostal.com` added to Vercel Production env, redeployed) since it directly blocked this task's own "land on the order-confirmed page" verification requirement and the app already fully supported the env var. Re-verified with fresh Playwright sessions against real production for all 3 demos after that redeploy: print-shop (Embroidered Zip Pouch, 16.00 USD), northline (Video Doorbell Installation, 129.00 USD), and broadleaf (Snake Plant, 22.00 USD) each completed a real add-to-cart -> checkout -> sandbox-Pay -> `/demo/{slug}/order/confirmed` flow with the correct `https://commerce.mdostal.com` success/cancel URLs and zero 500s. | **done -- real root cause found (untyped-parameter unary-minus SQL ambiguity in the Postgres inventory adapter, "operator is not unique: - unknown"), fixed, real-Postgres regression test added, merged, deployed, live-verified end-to-end across all 3 demos; also closed a separate disclosed `APP_ORIGIN` gap that blocked full verification** | 58, 61 |
| 63 | `product-configurator` | **Added 2026-09-22, per the user's standing "adapter epics must land as live, per-demo provider variety" preference generalized to this epic's own showcase intent -- a real, live, interactive multi-axis variant picker, not just resilient resolver code.** Planned as 4 stories (`7a8c7e5`), 3 already independently built and verified this session before this closeout story ran: **pc-02** (`555478e`) seeded a real 2-axis product, `embroidered-performance-polo` (print-shop demo) -- 2 colors (navy, charcoal-heather) x 3 sizes (small/medium/large) = 6 real priced/stocked SKUs, no synthetic placeholder data. **pc-01** (`8bf09ce`) built the real interactive variant-picker component (`components/variant-picker.tsx`) -- one real `<select>` per identifying-attribute key, GET-form progressive enhancement (works with client JS disabled via a `<noscript>` submit button), wired into all 3 PDP templates (`pdp-long-scroll.tsx`, `pdp-spec-sheet.tsx`, `pdp-tabbed-detail.tsx`) behind each template's own `skus.length > 1` guard -- giving `pdp.resolveSelection`/`catalog.resolveVariant` their first real, live call site; a single-SKU product never mounts the picker at all, preserving byte-identical rendering for the existing catalog. **pc-03** (`fc21751`) added the admin SKU-matrix view + add-combination surface at `/admin/products/[productId]/skus`, gated by `requireAdminPermission("mutate")`. **This story (pc-04):** ran the full monorepo check (`pnpm turbo run typecheck test build --force`) clean -- 127/127 tasks passed, zero failures, zero skips beyond the usual `TEST_DATABASE_URL`-gated integration suites. Merged `feat/product-configurator` to `master` for real (`--no-ff`, `fc21751` -> `e647371`) and pushed to `origin/master` immediately per this repo's standing public-repo push rule. Deployed live via `npx vercel@latest deploy --prod --yes --scope mathew-dostals-projects` (the `mercatus-liber-demo` Vercel project, linked at the repo root with its Root Directory pointed at `apps/reference-storefront`; the global `vercel` CLI's cached org context required an explicit `--scope` in non-interactive mode) -- deployment `dpl_3NVVAnAWE3koKoq67QDSEYdkBJNB`, aliased to `commerce.mdostal.com`. **Live verification, all against real production URLs, not local dev:** fetched the real PDP at `commerce.mdostal.com/demo/print-shop/products/embroidered-performance-polo` (200, real `vp-form`/`<select name="color">`/`<select name="size">` present); selected `?color=navy&size=medium` via the picker's own real query-param contract -- server-resolved price **44.00 USD, stock 24**, hidden `skuId` `56c04a8f-f9c0-49d6-9d1c-38934a1191a2`, exactly matching the seed's `navy`/`medium` SKU (`priceCents: 4400, stockUnits: 24`); separately selected `?color=charcoal-heather&size=large` -- server-resolved **47.00 USD, stock 10**, exactly matching that SKU's seed values (`priceCents: 4700, stockUnits: 10`) -- both real server-side `pdp.resolveSelection` resolutions, zero client-side matching logic. **Add-to-cart, verified with a real POST, not assumed:** submitted the real add-to-cart server-action form (same `skuId`/`demoSlug`/`quantity` fields a real browser submits) via `curl`, which set a real `ml_cart_id__print-shop` session cookie; fetching `/demo/print-shop/cart` with that cookie showed a real cart line for "Embroidered Performance Polo" carrying the identical `skuId` (`56c04a8f-...`) and price (44.00) just resolved -- the cart line matches the exactly-resolved SKU, live. **Admin SKU-matrix, live and correctly gated:** a bare `curl` (no browser signals) got a 404 from Clerk's own bot-detection rewrite (`x-clerk-auth-reason: protect-rewrite, dev-browser-missing`) -- confirmed this is identical, pre-existing behavior for every `/admin` route in the app (`admin/orders`, bare `/admin`), not specific to the new route; re-tested with real browser headers (`Accept`, `User-Agent`, `Sec-Fetch-*`) and a cookie jar preserved across the full redirect chain, and the SKU-matrix URL correctly resolved to Clerk's real hosted sign-in page (`more-lark-7491.accounts.dev/sign-in`, HTTP 200) carrying the exact `/demo/print-shop/admin/products/dc35b196-.../skus` path as its `redirect_url` -- routed and gated correctly, not a 404/500, for a genuine unauthenticated browser session. **Regression spot-checks, all live, all 200:** print-shop single-SKU products (`embroidered-canvas-tote`, `embroidered-dad-cap` -- confirmed zero `vp-form` markup, so the picker correctly never mounts on non-variant products), northline (`tv-wall-mounting`, `home-theater-setup`), broadleaf (`trailing-pothos`), all 3 demo home pages, and the framework root -- unaffected by this epic. | **done -- merged, pushed, deployed live, and live-verified end-to-end on real production URLs: real 2-axis picker resolves real per-combination price/stock server-side, a real add-to-cart POST lands the exactly-resolved SKU in a real cart, the admin SKU-matrix route is confirmed correctly routed and Clerk-gated (redirects to real hosted sign-in, not 404/500), and every other product/demo spot-checked is unaffected** | 1, 3, 6, 28 |

## Execution mode
Driven via a self-pacing loop (`/loop`) cycling `/plugin-hive:plan` (per epic, when not yet
planned) → `/plugin-hive:execute` (per epic's stories) → next epic. Collaborative-review gates
and per-document sign-off gates are run internally by the writer/team as usual, but user-facing
sign-off is only surfaced for the real-decision categories above — routine ceremony
confirmations are not escalated, per explicit instruction (2026-09-07).
